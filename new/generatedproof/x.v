(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.fmt.
Require Export New.generatedproof.strings.
Require Export New.generatedproof.sync.
Require Export New.generatedproof.sync.atomic.
Require Export New.golang.theory.
Require Export New.code.x.

Set Default Proof Using "Type".

Module main.
Module Worker.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : main.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Worker_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (main.Worker.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "id" ∷ l.[(main.Worker.t), "id"] ↦{dq} v.(main.Worker.id') ∗
      "queue" ∷ l.[(main.Worker.t), "queue"] ↦{dq} v.(main.Worker.queue') ∗
      "steal" ∷ l.[(main.Worker.t), "steal"] ↦{dq} v.(main.Worker.steal') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Worker_into_val_typed
   :
  IntoValTypedUnderlying (main.Worker.t) (main.Workerⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Worker_access_load_id l (v : (main.Worker.t)) dq :
  AccessStrict
    (l.[(main.Worker.t), "id"] ↦{dq} (v.(main.Worker.id')))
    (l.[(main.Worker.t), "id"] ↦{dq} (v.(main.Worker.id')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Worker_access_store_id l (v : (main.Worker.t)) id' :
  AccessStrict
    (l.[(main.Worker.t), "id"] ↦ (v.(main.Worker.id')))
    (l.[(main.Worker.t), "id"] ↦ id')
    (l ↦ v) (l ↦ (v <|(main.Worker.id') := id'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Worker_access_load_queue l (v : (main.Worker.t)) dq :
  AccessStrict
    (l.[(main.Worker.t), "queue"] ↦{dq} (v.(main.Worker.queue')))
    (l.[(main.Worker.t), "queue"] ↦{dq} (v.(main.Worker.queue')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Worker_access_store_queue l (v : (main.Worker.t)) queue' :
  AccessStrict
    (l.[(main.Worker.t), "queue"] ↦ (v.(main.Worker.queue')))
    (l.[(main.Worker.t), "queue"] ↦ queue')
    (l ↦ v) (l ↦ (v <|(main.Worker.queue') := queue'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Worker_access_load_steal l (v : (main.Worker.t)) dq :
  AccessStrict
    (l.[(main.Worker.t), "steal"] ↦{dq} (v.(main.Worker.steal')))
    (l.[(main.Worker.t), "steal"] ↦{dq} (v.(main.Worker.steal')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Worker_access_store_steal l (v : (main.Worker.t)) steal' :
  AccessStrict
    (l.[(main.Worker.t), "steal"] ↦ (v.(main.Worker.steal')))
    (l.[(main.Worker.t), "steal"] ↦ steal')
    (l ↦ v) (l ↦ (v <|(main.Worker.steal') := steal'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Worker.

End main.
