(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.disk_prelude.
Require Export New.generatedproof.github_com.goose_lang.primitive.disk.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.addr.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.common.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.util.
Require Export New.generatedproof.github_com.tchajed.marshal.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.go_journal.buf.

Set Default Proof Using "Type".

Module buf.
Module Buf.
Section def.

Context `{!heapGS Σ}.
Context {sem : go.Semantics}.
Context {package_sem' : buf.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Buf_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (buf.Buf.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "Addr" ∷ l.[(buf.Buf.t), "Addr"] ↦{dq} v.(buf.Buf.Addr') ∗
      "Sz" ∷ l.[(buf.Buf.t), "Sz"] ↦{dq} v.(buf.Buf.Sz') ∗
      "Data" ∷ l.[(buf.Buf.t), "Data"] ↦{dq} v.(buf.Buf.Data') ∗
      "dirty" ∷ l.[(buf.Buf.t), "dirty"] ↦{dq} v.(buf.Buf.dirty') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Buf_into_val_typed
   :
  IntoValTypedUnderlying (buf.Buf.t) (buf.Bufⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Buf_access_load_Addr l (v : (buf.Buf.t)) dq :
  AccessStrict
    (l.[(buf.Buf.t), "Addr"] ↦{dq} (v.(buf.Buf.Addr')))
    (l.[(buf.Buf.t), "Addr"] ↦{dq} (v.(buf.Buf.Addr')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Buf_access_store_Addr l (v : (buf.Buf.t)) Addr' :
  AccessStrict
    (l.[(buf.Buf.t), "Addr"] ↦ (v.(buf.Buf.Addr')))
    (l.[(buf.Buf.t), "Addr"] ↦ Addr')
    (l ↦ v) (l ↦ (v <|(buf.Buf.Addr') := Addr'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Buf_access_load_Sz l (v : (buf.Buf.t)) dq :
  AccessStrict
    (l.[(buf.Buf.t), "Sz"] ↦{dq} (v.(buf.Buf.Sz')))
    (l.[(buf.Buf.t), "Sz"] ↦{dq} (v.(buf.Buf.Sz')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Buf_access_store_Sz l (v : (buf.Buf.t)) Sz' :
  AccessStrict
    (l.[(buf.Buf.t), "Sz"] ↦ (v.(buf.Buf.Sz')))
    (l.[(buf.Buf.t), "Sz"] ↦ Sz')
    (l ↦ v) (l ↦ (v <|(buf.Buf.Sz') := Sz'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Buf_access_load_Data l (v : (buf.Buf.t)) dq :
  AccessStrict
    (l.[(buf.Buf.t), "Data"] ↦{dq} (v.(buf.Buf.Data')))
    (l.[(buf.Buf.t), "Data"] ↦{dq} (v.(buf.Buf.Data')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Buf_access_store_Data l (v : (buf.Buf.t)) Data' :
  AccessStrict
    (l.[(buf.Buf.t), "Data"] ↦ (v.(buf.Buf.Data')))
    (l.[(buf.Buf.t), "Data"] ↦ Data')
    (l ↦ v) (l ↦ (v <|(buf.Buf.Data') := Data'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Buf_access_load_dirty l (v : (buf.Buf.t)) dq :
  AccessStrict
    (l.[(buf.Buf.t), "dirty"] ↦{dq} (v.(buf.Buf.dirty')))
    (l.[(buf.Buf.t), "dirty"] ↦{dq} (v.(buf.Buf.dirty')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Buf_access_store_dirty l (v : (buf.Buf.t)) dirty' :
  AccessStrict
    (l.[(buf.Buf.t), "dirty"] ↦ (v.(buf.Buf.dirty')))
    (l.[(buf.Buf.t), "dirty"] ↦ dirty')
    (l ↦ v) (l ↦ (v <|(buf.Buf.dirty') := dirty'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Buf.

Module BufMap.
Section def.

Context `{!heapGS Σ}.
Context {sem : go.Semantics}.
Context {package_sem' : buf.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance BufMap_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (buf.BufMap.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "addrs" ∷ l.[(buf.BufMap.t), "addrs"] ↦{dq} v.(buf.BufMap.addrs') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance BufMap_into_val_typed
   :
  IntoValTypedUnderlying (buf.BufMap.t) (buf.BufMapⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance BufMap_access_load_addrs l (v : (buf.BufMap.t)) dq :
  AccessStrict
    (l.[(buf.BufMap.t), "addrs"] ↦{dq} (v.(buf.BufMap.addrs')))
    (l.[(buf.BufMap.t), "addrs"] ↦{dq} (v.(buf.BufMap.addrs')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance BufMap_access_store_addrs l (v : (buf.BufMap.t)) addrs' :
  AccessStrict
    (l.[(buf.BufMap.t), "addrs"] ↦ (v.(buf.BufMap.addrs')))
    (l.[(buf.BufMap.t), "addrs"] ↦ addrs')
    (l ↦ v) (l ↦ (v <|(buf.BufMap.addrs') := addrs'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End BufMap.

End buf.
