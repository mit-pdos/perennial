(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.disk_prelude.
Require Export New.generatedproof.github_com.goose_lang.primitive.disk.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.addr.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.common.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.jrnl.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.obj.
Require Export New.generatedproof.github_com.mit_pdos.go_journal.util.
Require Export New.generatedproof.sync.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.go_journal.jrnl_replication.

Set Default Proof Using "Type".

Module replicated_block.
Module RepBlock.
Section def.

Context `{!heapGS Σ}.
Context {sem : go.Semantics}.
Context {package_sem' : replicated_block.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance RepBlock_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (replicated_block.RepBlock.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "txn" ∷ l.[(replicated_block.RepBlock.t), "txn"] ↦{dq} v.(replicated_block.RepBlock.txn') ∗
      "m" ∷ l.[(replicated_block.RepBlock.t), "m"] ↦{dq} v.(replicated_block.RepBlock.m') ∗
      "a0" ∷ l.[(replicated_block.RepBlock.t), "a0"] ↦{dq} v.(replicated_block.RepBlock.a0') ∗
      "a1" ∷ l.[(replicated_block.RepBlock.t), "a1"] ↦{dq} v.(replicated_block.RepBlock.a1') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance RepBlock_into_val_typed
   :
  IntoValTypedUnderlying (replicated_block.RepBlock.t) (replicated_block.RepBlockⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance RepBlock_access_load_txn l (v : (replicated_block.RepBlock.t)) dq :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "txn"] ↦{dq} (v.(replicated_block.RepBlock.txn')))
    (l.[(replicated_block.RepBlock.t), "txn"] ↦{dq} (v.(replicated_block.RepBlock.txn')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance RepBlock_access_store_txn l (v : (replicated_block.RepBlock.t)) txn' :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "txn"] ↦ (v.(replicated_block.RepBlock.txn')))
    (l.[(replicated_block.RepBlock.t), "txn"] ↦ txn')
    (l ↦ v) (l ↦ (v <|(replicated_block.RepBlock.txn') := txn'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance RepBlock_access_load_m l (v : (replicated_block.RepBlock.t)) dq :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "m"] ↦{dq} (v.(replicated_block.RepBlock.m')))
    (l.[(replicated_block.RepBlock.t), "m"] ↦{dq} (v.(replicated_block.RepBlock.m')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance RepBlock_access_store_m l (v : (replicated_block.RepBlock.t)) m' :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "m"] ↦ (v.(replicated_block.RepBlock.m')))
    (l.[(replicated_block.RepBlock.t), "m"] ↦ m')
    (l ↦ v) (l ↦ (v <|(replicated_block.RepBlock.m') := m'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance RepBlock_access_load_a0 l (v : (replicated_block.RepBlock.t)) dq :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "a0"] ↦{dq} (v.(replicated_block.RepBlock.a0')))
    (l.[(replicated_block.RepBlock.t), "a0"] ↦{dq} (v.(replicated_block.RepBlock.a0')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance RepBlock_access_store_a0 l (v : (replicated_block.RepBlock.t)) a0' :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "a0"] ↦ (v.(replicated_block.RepBlock.a0')))
    (l.[(replicated_block.RepBlock.t), "a0"] ↦ a0')
    (l ↦ v) (l ↦ (v <|(replicated_block.RepBlock.a0') := a0'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance RepBlock_access_load_a1 l (v : (replicated_block.RepBlock.t)) dq :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "a1"] ↦{dq} (v.(replicated_block.RepBlock.a1')))
    (l.[(replicated_block.RepBlock.t), "a1"] ↦{dq} (v.(replicated_block.RepBlock.a1')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance RepBlock_access_store_a1 l (v : (replicated_block.RepBlock.t)) a1' :
  AccessStrict
    (l.[(replicated_block.RepBlock.t), "a1"] ↦ (v.(replicated_block.RepBlock.a1')))
    (l.[(replicated_block.RepBlock.t), "a1"] ↦ a1')
    (l ↦ v) (l ↦ (v <|(replicated_block.RepBlock.a1') := a1'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End RepBlock.

End replicated_block.
