(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.time.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.perennial.goose.testdata.examples.channel.lock.

Set Default Proof Using "Type".

Module lock.
Module Lock.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : lock.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Lock_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (lock.Lock.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "ch" ∷ l.[(lock.Lock.t), "ch"] ↦{dq} v.(lock.Lock.ch') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Lock_into_val_typed
   :
  IntoValTypedUnderlying (lock.Lock.t) (lock.Lockⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Lock_access_load_ch l (v : (lock.Lock.t)) dq :
  AccessStrict
    (l.[(lock.Lock.t), "ch"] ↦{dq} (v.(lock.Lock.ch')))
    (l.[(lock.Lock.t), "ch"] ↦{dq} (v.(lock.Lock.ch')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Lock_access_store_ch l (v : (lock.Lock.t)) ch' :
  AccessStrict
    (l.[(lock.Lock.t), "ch"] ↦ (v.(lock.Lock.ch')))
    (l.[(lock.Lock.t), "ch"] ↦ ch')
    (l ↦ v) (l ↦ (v <|(lock.Lock.ch') := ch'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Lock.

End lock.
