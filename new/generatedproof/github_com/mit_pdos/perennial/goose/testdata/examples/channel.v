(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.github_com.mit_pdos.perennial.goose.testdata.examples.channel.lock.
Require Export New.generatedproof.strings.
Require Export New.generatedproof.time.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.perennial.goose.testdata.examples.channel.

Set Default Proof Using "Type".

Module channel_examples.
Module Cond.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : channel_examples.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Cond_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (channel_examples.Cond.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "L" ∷ l.[(channel_examples.Cond.t), "L"] ↦{dq} v.(channel_examples.Cond.L') ∗
      "waiters" ∷ l.[(channel_examples.Cond.t), "waiters"] ↦{dq} v.(channel_examples.Cond.waiters') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Cond_into_val_typed
   :
  IntoValTypedUnderlying (channel_examples.Cond.t) (channel_examples.Condⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Cond_access_load_L l (v : (channel_examples.Cond.t)) dq :
  AccessStrict
    (l.[(channel_examples.Cond.t), "L"] ↦{dq} (v.(channel_examples.Cond.L')))
    (l.[(channel_examples.Cond.t), "L"] ↦{dq} (v.(channel_examples.Cond.L')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Cond_access_store_L l (v : (channel_examples.Cond.t)) L' :
  AccessStrict
    (l.[(channel_examples.Cond.t), "L"] ↦ (v.(channel_examples.Cond.L')))
    (l.[(channel_examples.Cond.t), "L"] ↦ L')
    (l ↦ v) (l ↦ (v <|(channel_examples.Cond.L') := L'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Cond_access_load_waiters l (v : (channel_examples.Cond.t)) dq :
  AccessStrict
    (l.[(channel_examples.Cond.t), "waiters"] ↦{dq} (v.(channel_examples.Cond.waiters')))
    (l.[(channel_examples.Cond.t), "waiters"] ↦{dq} (v.(channel_examples.Cond.waiters')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Cond_access_store_waiters l (v : (channel_examples.Cond.t)) waiters' :
  AccessStrict
    (l.[(channel_examples.Cond.t), "waiters"] ↦ (v.(channel_examples.Cond.waiters')))
    (l.[(channel_examples.Cond.t), "waiters"] ↦ waiters')
    (l ↦ v) (l ↦ (v <|(channel_examples.Cond.waiters') := waiters'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Cond.

Module Result.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : channel_examples.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Result_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (channel_examples.Result.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "value" ∷ l.[(channel_examples.Result.t), "value"] ↦{dq} v.(channel_examples.Result.value') ∗
      "primary_won" ∷ l.[(channel_examples.Result.t), "primary_won"] ↦{dq} v.(channel_examples.Result.primary_won') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Result_into_val_typed
   :
  IntoValTypedUnderlying (channel_examples.Result.t) (channel_examples.Resultⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Result_access_load_value l (v : (channel_examples.Result.t)) dq :
  AccessStrict
    (l.[(channel_examples.Result.t), "value"] ↦{dq} (v.(channel_examples.Result.value')))
    (l.[(channel_examples.Result.t), "value"] ↦{dq} (v.(channel_examples.Result.value')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Result_access_store_value l (v : (channel_examples.Result.t)) value' :
  AccessStrict
    (l.[(channel_examples.Result.t), "value"] ↦ (v.(channel_examples.Result.value')))
    (l.[(channel_examples.Result.t), "value"] ↦ value')
    (l ↦ v) (l ↦ (v <|(channel_examples.Result.value') := value'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Result_access_load_primary_won l (v : (channel_examples.Result.t)) dq :
  AccessStrict
    (l.[(channel_examples.Result.t), "primary_won"] ↦{dq} (v.(channel_examples.Result.primary_won')))
    (l.[(channel_examples.Result.t), "primary_won"] ↦{dq} (v.(channel_examples.Result.primary_won')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Result_access_store_primary_won l (v : (channel_examples.Result.t)) primary_won' :
  AccessStrict
    (l.[(channel_examples.Result.t), "primary_won"] ↦ (v.(channel_examples.Result.primary_won')))
    (l.[(channel_examples.Result.t), "primary_won"] ↦ primary_won')
    (l ↦ v) (l ↦ (v <|(channel_examples.Result.primary_won') := primary_won'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Result.

Module request.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : channel_examples.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance request_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (channel_examples.request.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "f" ∷ l.[(channel_examples.request.t), "f"] ↦{dq} v.(channel_examples.request.f') ∗
      "result" ∷ l.[(channel_examples.request.t), "result"] ↦{dq} v.(channel_examples.request.result') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance request_into_val_typed
   :
  IntoValTypedUnderlying (channel_examples.request.t) (channel_examples.requestⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance request_access_load_f l (v : (channel_examples.request.t)) dq :
  AccessStrict
    (l.[(channel_examples.request.t), "f"] ↦{dq} (v.(channel_examples.request.f')))
    (l.[(channel_examples.request.t), "f"] ↦{dq} (v.(channel_examples.request.f')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance request_access_store_f l (v : (channel_examples.request.t)) f' :
  AccessStrict
    (l.[(channel_examples.request.t), "f"] ↦ (v.(channel_examples.request.f')))
    (l.[(channel_examples.request.t), "f"] ↦ f')
    (l ↦ v) (l ↦ (v <|(channel_examples.request.f') := f'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance request_access_load_result l (v : (channel_examples.request.t)) dq :
  AccessStrict
    (l.[(channel_examples.request.t), "result"] ↦{dq} (v.(channel_examples.request.result')))
    (l.[(channel_examples.request.t), "result"] ↦{dq} (v.(channel_examples.request.result')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance request_access_store_result l (v : (channel_examples.request.t)) result' :
  AccessStrict
    (l.[(channel_examples.request.t), "result"] ↦ (v.(channel_examples.request.result')))
    (l.[(channel_examples.request.t), "result"] ↦ result')
    (l ↦ v) (l ↦ (v <|(channel_examples.request.result') := result'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End request.

Module stream.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : channel_examples.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance stream_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (channel_examples.stream.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "req" ∷ l.[(channel_examples.stream.t), "req"] ↦{dq} v.(channel_examples.stream.req') ∗
      "res" ∷ l.[(channel_examples.stream.t), "res"] ↦{dq} v.(channel_examples.stream.res') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance stream_into_val_typed
   :
  IntoValTypedUnderlying (channel_examples.stream.t) (channel_examples.streamⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance stream_access_load_req l (v : (channel_examples.stream.t)) dq :
  AccessStrict
    (l.[(channel_examples.stream.t), "req"] ↦{dq} (v.(channel_examples.stream.req')))
    (l.[(channel_examples.stream.t), "req"] ↦{dq} (v.(channel_examples.stream.req')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance stream_access_store_req l (v : (channel_examples.stream.t)) req' :
  AccessStrict
    (l.[(channel_examples.stream.t), "req"] ↦ (v.(channel_examples.stream.req')))
    (l.[(channel_examples.stream.t), "req"] ↦ req')
    (l ↦ v) (l ↦ (v <|(channel_examples.stream.req') := req'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance stream_access_load_res l (v : (channel_examples.stream.t)) dq :
  AccessStrict
    (l.[(channel_examples.stream.t), "res"] ↦{dq} (v.(channel_examples.stream.res')))
    (l.[(channel_examples.stream.t), "res"] ↦{dq} (v.(channel_examples.stream.res')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance stream_access_store_res l (v : (channel_examples.stream.t)) res' :
  AccessStrict
    (l.[(channel_examples.stream.t), "res"] ↦ (v.(channel_examples.stream.res')))
    (l.[(channel_examples.stream.t), "res"] ↦ res')
    (l ↦ v) (l ↦ (v <|(channel_examples.stream.res') := res'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End stream.

Module streamold.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : channel_examples.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance streamold_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (channel_examples.streamold.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "req" ∷ l.[(channel_examples.streamold.t), "req"] ↦{dq} v.(channel_examples.streamold.req') ∗
      "res" ∷ l.[(channel_examples.streamold.t), "res"] ↦{dq} v.(channel_examples.streamold.res') ∗
      "f" ∷ l.[(channel_examples.streamold.t), "f"] ↦{dq} v.(channel_examples.streamold.f') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance streamold_into_val_typed
   :
  IntoValTypedUnderlying (channel_examples.streamold.t) (channel_examples.streamoldⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance streamold_access_load_req l (v : (channel_examples.streamold.t)) dq :
  AccessStrict
    (l.[(channel_examples.streamold.t), "req"] ↦{dq} (v.(channel_examples.streamold.req')))
    (l.[(channel_examples.streamold.t), "req"] ↦{dq} (v.(channel_examples.streamold.req')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance streamold_access_store_req l (v : (channel_examples.streamold.t)) req' :
  AccessStrict
    (l.[(channel_examples.streamold.t), "req"] ↦ (v.(channel_examples.streamold.req')))
    (l.[(channel_examples.streamold.t), "req"] ↦ req')
    (l ↦ v) (l ↦ (v <|(channel_examples.streamold.req') := req'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance streamold_access_load_res l (v : (channel_examples.streamold.t)) dq :
  AccessStrict
    (l.[(channel_examples.streamold.t), "res"] ↦{dq} (v.(channel_examples.streamold.res')))
    (l.[(channel_examples.streamold.t), "res"] ↦{dq} (v.(channel_examples.streamold.res')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance streamold_access_store_res l (v : (channel_examples.streamold.t)) res' :
  AccessStrict
    (l.[(channel_examples.streamold.t), "res"] ↦ (v.(channel_examples.streamold.res')))
    (l.[(channel_examples.streamold.t), "res"] ↦ res')
    (l ↦ v) (l ↦ (v <|(channel_examples.streamold.res') := res'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance streamold_access_load_f l (v : (channel_examples.streamold.t)) dq :
  AccessStrict
    (l.[(channel_examples.streamold.t), "f"] ↦{dq} (v.(channel_examples.streamold.f')))
    (l.[(channel_examples.streamold.t), "f"] ↦{dq} (v.(channel_examples.streamold.f')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance streamold_access_store_f l (v : (channel_examples.streamold.t)) f' :
  AccessStrict
    (l.[(channel_examples.streamold.t), "f"] ↦ (v.(channel_examples.streamold.f')))
    (l.[(channel_examples.streamold.t), "f"] ↦ f')
    (l ↦ v) (l ↦ (v <|(channel_examples.streamold.f') := f'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End streamold.

End channel_examples.
