(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.github_com.tchajed.marshal.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.gokv.memkv.shardmap_gk.

Set Default Proof Using "Type".

Module shardmap_gk.
Module S.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : shardmap_gk.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance S_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (shardmap_gk.S.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "Shards" ∷ l.[(shardmap_gk.S.t), "Shards"] ↦{dq} v.(shardmap_gk.S.Shards') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance S_into_val_typed
   :
  IntoValTypedUnderlying (shardmap_gk.S.t) (shardmap_gk.Sⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance S_access_load_Shards l (v : (shardmap_gk.S.t)) dq :
  AccessStrict
    (l.[(shardmap_gk.S.t), "Shards"] ↦{dq} (v.(shardmap_gk.S.Shards')))
    (l.[(shardmap_gk.S.t), "Shards"] ↦{dq} (v.(shardmap_gk.S.Shards')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance S_access_store_Shards l (v : (shardmap_gk.S.t)) Shards' :
  AccessStrict
    (l.[(shardmap_gk.S.t), "Shards"] ↦ (v.(shardmap_gk.S.Shards')))
    (l.[(shardmap_gk.S.t), "Shards"] ↦ Shards')
    (l ↦ v) (l ↦ (v <|(shardmap_gk.S.Shards') := Shards'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End S.

End shardmap_gk.
