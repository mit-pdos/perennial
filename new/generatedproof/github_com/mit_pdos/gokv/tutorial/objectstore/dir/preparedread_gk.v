(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.github_com.mit_pdos.gokv.tutorial.objectstore.dir.chunkhandle_gk.
Require Export New.generatedproof.github_com.tchajed.marshal.
Require Export New.golang.theory.
Require Export New.code.github_com.mit_pdos.gokv.tutorial.objectstore.dir.preparedread_gk.

Set Default Proof Using "Type".

Module preparedread_gk.
Module S.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : preparedread_gk.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance S_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (preparedread_gk.S.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "Handles" ∷ l.[(preparedread_gk.S.t), "Handles"] ↦{dq} v.(preparedread_gk.S.Handles') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance S_into_val_typed
   :
  IntoValTypedUnderlying (preparedread_gk.S.t) (preparedread_gk.Sⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance S_access_load_Handles l (v : (preparedread_gk.S.t)) dq :
  AccessStrict
    (l.[(preparedread_gk.S.t), "Handles"] ↦{dq} (v.(preparedread_gk.S.Handles')))
    (l.[(preparedread_gk.S.t), "Handles"] ↦{dq} (v.(preparedread_gk.S.Handles')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance S_access_store_Handles l (v : (preparedread_gk.S.t)) Handles' :
  AccessStrict
    (l.[(preparedread_gk.S.t), "Handles"] ↦ (v.(preparedread_gk.S.Handles')))
    (l.[(preparedread_gk.S.t), "Handles"] ↦ Handles')
    (l ↦ v) (l ↦ (v <|(preparedread_gk.S.Handles') := Handles'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End S.

End preparedread_gk.
