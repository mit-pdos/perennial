(* autogenerated by goose proofgen; do not modify *)
Require Export New.proof.proof_prelude.
Require Export New.generatedproof.strings.
Require Export New.generatedproof.sync.atomic.
Require Export New.golang.theory.
Require Export New.code.github_com.goose_lang.goose.testdata.examples.channel.workq.

Set Default Proof Using "Type".

Module workq.
Module Worker.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : workq.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance Worker_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (workq.Worker.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "queue" ∷ l.[(workq.Worker.t), "queue"] ↦{dq} v.(workq.Worker.queue') ∗
      "steal" ∷ l.[(workq.Worker.t), "steal"] ↦{dq} v.(workq.Worker.steal') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance Worker_into_val_typed
   :
  IntoValTypedUnderlying (workq.Worker.t) (workq.Workerⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance Worker_access_load_queue l (v : (workq.Worker.t)) dq :
  AccessStrict
    (l.[(workq.Worker.t), "queue"] ↦{dq} (v.(workq.Worker.queue')))
    (l.[(workq.Worker.t), "queue"] ↦{dq} (v.(workq.Worker.queue')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Worker_access_store_queue l (v : (workq.Worker.t)) queue' :
  AccessStrict
    (l.[(workq.Worker.t), "queue"] ↦ (v.(workq.Worker.queue')))
    (l.[(workq.Worker.t), "queue"] ↦ queue')
    (l ↦ v) (l ↦ (v <|(workq.Worker.queue') := queue'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance Worker_access_load_steal l (v : (workq.Worker.t)) dq :
  AccessStrict
    (l.[(workq.Worker.t), "steal"] ↦{dq} (v.(workq.Worker.steal')))
    (l.[(workq.Worker.t), "steal"] ↦{dq} (v.(workq.Worker.steal')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance Worker_access_store_steal l (v : (workq.Worker.t)) steal' :
  AccessStrict
    (l.[(workq.Worker.t), "steal"] ↦ (v.(workq.Worker.steal')))
    (l.[(workq.Worker.t), "steal"] ↦ steal')
    (l ↦ v) (l ↦ (v <|(workq.Worker.steal') := steal'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End Worker.

Module shared.
Section def.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context {sem : go.Semantics}.
Context {package_sem' : workq.Assumptions}.

Local Set Default Proof Using "All".

#[global]Program Instance shared_typed_pointsto  :
  TypedPointsto (Σ:=Σ) (workq.shared.t) :=
  {|
    typed_pointsto_def l v dq :=
      (
      "remaining" ∷ l.[(workq.shared.t), "remaining"] ↦{dq} v.(workq.shared.remaining') ∗
      "total" ∷ l.[(workq.shared.t), "total"] ↦{dq} v.(workq.shared.total') ∗
      "done" ∷ l.[(workq.shared.t), "done"] ↦{dq} v.(workq.shared.done') ∗
      "_" ∷ True
      )%I
  |}.
Final Obligation. solve_typed_pointsto_agree. Qed.

#[global] Instance shared_into_val_typed
   :
  IntoValTypedUnderlying (workq.shared.t) (workq.sharedⁱᵐᵖˡ).
Proof. solve_into_val_typed_struct. Qed.
#[global] Instance shared_access_load_remaining l (v : (workq.shared.t)) dq :
  AccessStrict
    (l.[(workq.shared.t), "remaining"] ↦{dq} (v.(workq.shared.remaining')))
    (l.[(workq.shared.t), "remaining"] ↦{dq} (v.(workq.shared.remaining')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance shared_access_store_remaining l (v : (workq.shared.t)) remaining' :
  AccessStrict
    (l.[(workq.shared.t), "remaining"] ↦ (v.(workq.shared.remaining')))
    (l.[(workq.shared.t), "remaining"] ↦ remaining')
    (l ↦ v) (l ↦ (v <|(workq.shared.remaining') := remaining'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance shared_access_load_total l (v : (workq.shared.t)) dq :
  AccessStrict
    (l.[(workq.shared.t), "total"] ↦{dq} (v.(workq.shared.total')))
    (l.[(workq.shared.t), "total"] ↦{dq} (v.(workq.shared.total')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance shared_access_store_total l (v : (workq.shared.t)) total' :
  AccessStrict
    (l.[(workq.shared.t), "total"] ↦ (v.(workq.shared.total')))
    (l.[(workq.shared.t), "total"] ↦ total')
    (l ↦ v) (l ↦ (v <|(workq.shared.total') := total'|>))%I.
Proof. solve_pointsto_access_struct. Qed.
#[global] Instance shared_access_load_done l (v : (workq.shared.t)) dq :
  AccessStrict
    (l.[(workq.shared.t), "done"] ↦{dq} (v.(workq.shared.done')))
    (l.[(workq.shared.t), "done"] ↦{dq} (v.(workq.shared.done')))
    (l ↦{dq} v) (l ↦{dq} v)%I.
Proof. solve_pointsto_access_struct. Qed.

#[global] Instance shared_access_store_done l (v : (workq.shared.t)) done' :
  AccessStrict
    (l.[(workq.shared.t), "done"] ↦ (v.(workq.shared.done')))
    (l.[(workq.shared.t), "done"] ↦ done')
    (l ↦ v) (l ↦ (v <|(workq.shared.done') := done'|>))%I.
Proof. solve_pointsto_access_struct. Qed.

End def.
End shared.

End workq.
