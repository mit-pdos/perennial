(*****************************************)
(* This file is autogenerated by grackle *)
(*    DO NOT MANUALLY EDIT THIS FILE     *)
(*****************************************)

From New.proof Require Import proof_prelude.
From Perennial.Helpers Require Import NamedProps.
From New.proof Require Import github_com.tchajed.marshal.
From New.proof Require Import github_com.goose_lang.primitive.
From New.proof Require Import github_com.goose_lang.std.
From New.code Require Import github_com.mit_pdos.gokv.tutorial.objectstore.dir.chunkhandle_gk.
From New.generatedproof Require Import github_com.mit_pdos.gokv.tutorial.objectstore.dir.chunkhandle_gk.

Module chunkHandle_gk.
Section chunkHandle_gk.

Context `{hG: heapGS Σ, !ffi_semantics _ _}.
Context `{!globalsGS Σ} {go_ctx : GoContext}.

#[global] Instance : IsPkgInit chunkhandle_gk := define_is_pkg_init True%I.

Definition C := chunkhandle_gk.S.t.

Definition has_encoding (encoded:list u8) (args:C) : Prop :=
  encoded = (u64_le args.(chunkhandle_gk.S.Addr')) ++
              (u64_le $ length $ args.(chunkhandle_gk.S.ContentHash')) ++ args.(chunkhandle_gk.S.ContentHash')
  /\ length args.(chunkhandle_gk.S.ContentHash') < 2^64.

Definition own (args__v: chunkhandle_gk.S.t) (args__c: C) (dq: dfrac) : iProp Σ :=
  "%Hown_addr" ∷ ⌜ args__v.(chunkhandle_gk.S.Addr') = args__c.(chunkhandle_gk.S.Addr') ⌝ ∗
  "%Hown_contentHash" ∷ ⌜ args__v.(chunkhandle_gk.S.ContentHash') = args__c.(chunkhandle_gk.S.ContentHash') ⌝ ∗
  "%Hown_contentHash_len" ∷ ⌜ length args__c.(chunkhandle_gk.S.ContentHash') < 2^64 ⌝.

Lemma wp_Encode (args__t : chunkhandle_gk.S.t) (args__c : C) (pre_sl : slice.t) (prefix : list u8) (dq : dfrac):
  {{{
        is_pkg_init chunkhandle_gk ∗
        own args__t args__c dq ∗ 
        own_slice pre_sl (DfracOwn 1) prefix ∗
        own_slice_cap w8 pre_sl (DfracOwn 1)
  }}}
    @! chunkhandle_gk.Marshal #pre_sl #args__t
  {{{
        enc enc_sl, RET #enc_sl;
        ⌜ has_encoding enc args__c ⌝ ∗
        own args__t args__c dq ∗ 
        own_slice enc_sl (DfracOwn 1) (prefix ++ enc) ∗
        own_slice_cap w8 enc_sl (DfracOwn 1)
  }}}.

Proof.
  wp_start as "(Hown & Hsl & Hcap)".
  iUnfold own in "Hown". iNamed "Hown".
  wp_auto.

  wp_apply (wp_WriteInt with "[$Hsl $Hcap]").
  iIntros (?) "[Hsl Hcap]". wp_auto.

  wp_apply wp_AssumeNoStringOverflow. iIntros "%HcontentHashLen". wp_auto.
  wp_apply wp_StringToBytes. iIntros (?) "HcontentHashBytes". wp_auto.
  wp_apply (wp_WriteLenPrefixedBytes with "[$Hsl $Hcap $HcontentHashBytes]").
  iIntros (?) "(Hsl & Hcap & HcontentHashBytes)". wp_auto.

  iApply "HΦ". rewrite -?app_assoc.
  iFrame. iPureIntro.

  unfold has_encoding.
  split; last done.
  
  split. all: congruence || done. 
Qed.

Lemma wp_Decode (enc : list u8) (enc_sl : slice.t) (args__c : C) (suffix : list u8) (dq : dfrac):
  {{{
        is_pkg_init chunkhandle_gk ∗
        ⌜ has_encoding enc args__c ⌝ ∗
        own_slice enc_sl dq (enc ++ suffix)
  }}}
    @! chunkhandle_gk.Unmarshal #enc_sl
  {{{
        args__t suff_sl, RET (#args__t, #suff_sl);
        own args__t args__c (DfracOwn 1) ∗ 
        own_slice suff_sl dq suffix
  }}}.

Proof.
  wp_start as "[%Henc Hsl]". wp_auto.
  unfold has_encoding in Henc.
  destruct Henc as (Henc & Hlen_contentHash ).
  rewrite Henc. rewrite -?app_assoc.

  wp_apply (wp_ReadInt with "[$Hsl]"). iIntros (?) "Hsl". wp_auto.

  wp_apply (wp_ReadLenPrefixedBytes with "[$Hsl]"); first word.
  iIntros (??) "[HcontentHash_byt Hsl]". wp_auto.
  wp_apply (wp_BytesClone with "[$HcontentHash_byt]").
  iIntros (?) "[HcontentHash_byt HcontentHash_byt_cap]".
  wp_apply (wp_StringFromBytes with "[$HcontentHash_byt]").
  iIntros "HcontentHash_byt". wp_auto.

  iApply "HΦ". iFrame; done.
Qed.

End chunkHandle_gk.
End chunkHandle_gk.

