(* autogenerated from github.com/mit-pdos/gokv/tutorial/objectstore/dir/preparedread_gk *)
Require Export New.code.github_com.mit_pdos.gokv.tutorial.objectstore.dir.chunkhandle_gk.
Require Export New.code.github_com.tchajed.marshal.

From New.golang Require Import defn.
Definition preparedread_gk : go_string := "github.com/mit-pdos/gokv/tutorial/objectstore/dir/preparedread_gk".

Module preparedread_gk.

Module S. Definition id : go_string := "github.com/mit-pdos/gokv/tutorial/objectstore/dir/preparedread_gk.S"%go. End S.

Section code.
Context `{ffi_syntax}.


Definition S : go_type := structT [
  "Handles" :: sliceT
].
#[global] Typeclasses Opaque S.
#[global] Opaque S.

Definition Marshal : go_string := "github.com/mit-pdos/gokv/tutorial/objectstore/dir/preparedread_gk.Marshal"%go.

(* go: preparedread_gk.go:18:6 *)
Definition Marshalⁱᵐᵖˡ : val :=
  λ: "enc" "p",
    exception_do (let: "p" := (mem.alloc "p") in
    let: "enc" := (mem.alloc "enc") in
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (s_to_w64 (let: "$a0" := (![#sliceT] (struct.field_ref #S #"Handles"%go "p")) in
    slice.len "$a0")) in
    (func_call #marshal.WriteInt) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#sliceT] (struct.field_ref #S #"Handles"%go "p")) in
    let: "$a2" := (func_call #chunkhandle_gk.Marshal) in
    (func_call #marshal.WriteSlice #chunkhandle_gk.S) "$a0" "$a1" "$a2") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    return: (![#sliceT] "enc")).

Definition Unmarshal : go_string := "github.com/mit-pdos/gokv/tutorial/objectstore/dir/preparedread_gk.Unmarshal"%go.

(* go: preparedread_gk.go:26:6 *)
Definition Unmarshalⁱᵐᵖˡ : val :=
  λ: "s",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "handlesLen" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadInt) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("handlesLen" <-[#uint64T] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "handles" := (mem.alloc (type.zero_val #sliceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    let: "$a1" := (![#uint64T] "handlesLen") in
    let: "$a2" := (func_call #chunkhandle_gk.Unmarshal) in
    (func_call #marshal.ReadSlice #chunkhandle_gk.S) "$a0" "$a1" "$a2") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("handles" <-[#sliceT] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    return: (let: "$Handles" := (![#sliceT] "handles") in
     struct.make #S [{
       "Handles" ::= "$Handles"
     }], ![#sliceT] "s")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(Marshal, Marshalⁱᵐᵖˡ); (Unmarshal, Unmarshalⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(S.id, []); (ptrT.id S.id, [])].

#[global] Instance info' : PkgInfo preparedread_gk.preparedread_gk :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.github_com.tchajed.marshal.marshal; code.github_com.mit_pdos.gokv.tutorial.objectstore.dir.chunkhandle_gk.chunkhandle_gk];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #preparedread_gk.preparedread_gk (λ: <>,
      exception_do (do:  (chunkhandle_gk.initialize' #());;;
      do:  (marshal.initialize' #());;;
      do:  (package.alloc preparedread_gk.preparedread_gk #()))
      ).

End code.
End preparedread_gk.
