(* autogenerated from github.com/mit-pdos/gokv/vrsm/apps/vkv/putargs_gk *)
Require Export New.code.github_com.goose_lang.primitive.
Require Export New.code.github_com.goose_lang.std.
Require Export New.code.github_com.tchajed.marshal.

From New.golang Require Import defn.
Definition putargs_gk : go_string := "github.com/mit-pdos/gokv/vrsm/apps/vkv/putargs_gk".

Module putargs_gk.

Module S. Definition id : go_string := "github.com/mit-pdos/gokv/vrsm/apps/vkv/putargs_gk.S"%go. End S.

Section code.
Context `{ffi_syntax}.


Definition S : go_type := structT [
  "Key" :: stringT;
  "Val" :: stringT
].
#[global] Typeclasses Opaque S.
#[global] Opaque S.

Definition Marshal : go_string := "github.com/mit-pdos/gokv/vrsm/apps/vkv/putargs_gk.Marshal"%go.

(* go: putargs_gk.go:19:6 *)
Definition Marshalⁱᵐᵖˡ : val :=
  λ: "enc" "p",
    exception_do (let: "p" := (mem.alloc "p") in
    let: "enc" := (mem.alloc "enc") in
    do:  (let: "$a0" := (![#stringT] (struct.field_ref #S #"Key"%go "p")) in
    (func_call #primitive.AssumeNoStringOverflow) "$a0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (string.to_bytes (![#stringT] (struct.field_ref #S #"Key"%go "p"))) in
    (func_call #marshal.WriteLenPrefixedBytes) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#stringT] (struct.field_ref #S #"Val"%go "p")) in
    (func_call #primitive.AssumeNoStringOverflow) "$a0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (string.to_bytes (![#stringT] (struct.field_ref #S #"Val"%go "p"))) in
    (func_call #marshal.WriteLenPrefixedBytes) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    return: (![#sliceT] "enc")).

Definition Unmarshal : go_string := "github.com/mit-pdos/gokv/vrsm/apps/vkv/putargs_gk.Unmarshal"%go.

(* go: putargs_gk.go:28:6 *)
Definition Unmarshalⁱᵐᵖˡ : val :=
  λ: "s",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "keyBytes" := (mem.alloc (type.zero_val #sliceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadLenPrefixedBytes) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("keyBytes" <-[#sliceT] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "key" := (mem.alloc (type.zero_val #stringT)) in
    let: "$r0" := (string.from_bytes (let: "$a0" := (![#sliceT] "keyBytes") in
    (func_call #std.BytesClone) "$a0")) in
    do:  ("key" <-[#stringT] "$r0");;;
    let: "valBytes" := (mem.alloc (type.zero_val #sliceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadLenPrefixedBytes) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("valBytes" <-[#sliceT] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "val" := (mem.alloc (type.zero_val #stringT)) in
    let: "$r0" := (string.from_bytes (let: "$a0" := (![#sliceT] "valBytes") in
    (func_call #std.BytesClone) "$a0")) in
    do:  ("val" <-[#stringT] "$r0");;;
    return: (let: "$Key" := (![#stringT] "key") in
     let: "$Val" := (![#stringT] "val") in
     struct.make #S [{
       "Key" ::= "$Key";
       "Val" ::= "$Val"
     }], ![#sliceT] "s")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(Marshal, Marshalⁱᵐᵖˡ); (Unmarshal, Unmarshalⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(S.id, []); (ptrT.id S.id, [])].

#[global] Instance info' : PkgInfo putargs_gk.putargs_gk :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.github_com.goose_lang.primitive.primitive; code.github_com.goose_lang.std.std; code.github_com.tchajed.marshal.marshal];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #putargs_gk.putargs_gk (λ: <>,
      exception_do (do:  (marshal.initialize' #());;;
      do:  (std.initialize' #());;;
      do:  (primitive.initialize' #());;;
      do:  (package.alloc putargs_gk.putargs_gk #()))
      ).

End code.
End putargs_gk.
