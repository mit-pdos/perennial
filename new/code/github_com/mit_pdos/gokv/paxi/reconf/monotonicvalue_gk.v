(* autogenerated from github.com/mit-pdos/gokv/paxi/reconf/monotonicvalue_gk *)
Require Export New.code.github_com.goose_lang.std.
Require Export New.code.github_com.mit_pdos.gokv.paxi.reconf.config_gk.
Require Export New.code.github_com.tchajed.marshal.

From New.golang Require Import defn.
Definition monotonicvalue_gk : go_string := "github.com/mit-pdos/gokv/paxi/reconf/monotonicvalue_gk".

Module monotonicvalue_gk.

Module S. Definition id : go_string := "github.com/mit-pdos/gokv/paxi/reconf/monotonicvalue_gk.S"%go. End S.

Section code.
Context `{ffi_syntax}.


Definition S : go_type := structT [
  "Version" :: uint64T;
  "Val" :: sliceT;
  "Conf" :: config_gk.S
].
#[global] Typeclasses Opaque S.
#[global] Opaque S.

Definition Marshal : go_string := "github.com/mit-pdos/gokv/paxi/reconf/monotonicvalue_gk.Marshal"%go.

(* go: monotonicvalue_gk.go:21:6 *)
Definition Marshalⁱᵐᵖˡ : val :=
  λ: "enc" "m",
    exception_do (let: "m" := (mem.alloc "m") in
    let: "enc" := (mem.alloc "enc") in
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#uint64T] (struct.field_ref #S #"Version"%go "m")) in
    (func_call #marshal.WriteInt) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (s_to_w64 (let: "$a0" := (![#sliceT] (struct.field_ref #S #"Val"%go "m")) in
    slice.len "$a0")) in
    (func_call #marshal.WriteInt) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#sliceT] (struct.field_ref #S #"Val"%go "m")) in
    (func_call #marshal.WriteBytes) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#config_gk.S] (struct.field_ref #S #"Conf"%go "m")) in
    (func_call #config_gk.Marshal) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    return: (![#sliceT] "enc")).

Definition Unmarshal : go_string := "github.com/mit-pdos/gokv/paxi/reconf/monotonicvalue_gk.Unmarshal"%go.

(* go: monotonicvalue_gk.go:30:6 *)
Definition Unmarshalⁱᵐᵖˡ : val :=
  λ: "s",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "version" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadInt) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("version" <-[#uint64T] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "valBytes" := (mem.alloc (type.zero_val #sliceT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadLenPrefixedBytes) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("valBytes" <-[#sliceT] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "val" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "valBytes") in
    (func_call #std.BytesClone) "$a0") in
    do:  ("val" <-[#sliceT] "$r0");;;
    let: "conf" := (mem.alloc (type.zero_val #config_gk.S)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #config_gk.Unmarshal) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("conf" <-[#config_gk.S] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    return: (let: "$Version" := (![#uint64T] "version") in
     let: "$Val" := (![#sliceT] "val") in
     let: "$Conf" := (![#config_gk.S] "conf") in
     struct.make #S [{
       "Version" ::= "$Version";
       "Val" ::= "$Val";
       "Conf" ::= "$Conf"
     }], ![#sliceT] "s")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(Marshal, Marshalⁱᵐᵖˡ); (Unmarshal, Unmarshalⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(S.id, []); (ptrT.id S.id, [])].

#[global] Instance info' : PkgInfo monotonicvalue_gk.monotonicvalue_gk :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.github_com.goose_lang.std.std; code.github_com.tchajed.marshal.marshal; code.github_com.mit_pdos.gokv.paxi.reconf.config_gk.config_gk];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #monotonicvalue_gk.monotonicvalue_gk (λ: <>,
      exception_do (do:  (config_gk.initialize' #());;;
      do:  (marshal.initialize' #());;;
      do:  (std.initialize' #());;;
      do:  (package.alloc monotonicvalue_gk.monotonicvalue_gk #()))
      ).

End code.
End monotonicvalue_gk.
