(* autogenerated from github.com/mit-pdos/gokv/reconfig/replica/appendargs_gk *)
Require Export New.code.github_com.mit_pdos.gokv.reconfig.replica.logentry_gk.
Require Export New.code.github_com.tchajed.marshal.

From New.golang Require Import defn.
Definition appendargs_gk : go_string := "github.com/mit-pdos/gokv/reconfig/replica/appendargs_gk".

Module appendargs_gk.

Module S. Definition id : go_string := "github.com/mit-pdos/gokv/reconfig/replica/appendargs_gk.S"%go. End S.

Section code.
Context `{ffi_syntax}.


Definition S : go_type := structT [
  "Epoch" :: uint64T;
  "Entry" :: logentry_gk.S;
  "Index" :: uint64T
].
#[global] Typeclasses Opaque S.
#[global] Opaque S.

Definition Marshal : go_string := "github.com/mit-pdos/gokv/reconfig/replica/appendargs_gk.Marshal"%go.

(* go: appendargs_gk.go:20:6 *)
Definition Marshalⁱᵐᵖˡ : val :=
  λ: "enc" "a",
    exception_do (let: "a" := (mem.alloc "a") in
    let: "enc" := (mem.alloc "enc") in
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#uint64T] (struct.field_ref #S #"Epoch"%go "a")) in
    (func_call #marshal.WriteInt) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#logentry_gk.S] (struct.field_ref #S #"Entry"%go "a")) in
    (func_call #logentry_gk.Marshal) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#sliceT] "enc") in
    let: "$a1" := (![#uint64T] (struct.field_ref #S #"Index"%go "a")) in
    (func_call #marshal.WriteInt) "$a0" "$a1") in
    do:  ("enc" <-[#sliceT] "$r0");;;
    return: (![#sliceT] "enc")).

Definition Unmarshal : go_string := "github.com/mit-pdos/gokv/reconfig/replica/appendargs_gk.Unmarshal"%go.

(* go: appendargs_gk.go:28:6 *)
Definition Unmarshalⁱᵐᵖˡ : val :=
  λ: "s",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "epoch" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadInt) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("epoch" <-[#uint64T] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "entry" := (mem.alloc (type.zero_val #logentry_gk.S)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #logentry_gk.Unmarshal) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("entry" <-[#logentry_gk.S] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    let: "index" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "s") in
    (func_call #marshal.ReadInt) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("index" <-[#uint64T] "$r0");;;
    do:  ("s" <-[#sliceT] "$r1");;;
    return: (let: "$Epoch" := (![#uint64T] "epoch") in
     let: "$Entry" := (![#logentry_gk.S] "entry") in
     let: "$Index" := (![#uint64T] "index") in
     struct.make #S [{
       "Epoch" ::= "$Epoch";
       "Entry" ::= "$Entry";
       "Index" ::= "$Index"
     }], ![#sliceT] "s")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [(Marshal, Marshalⁱᵐᵖˡ); (Unmarshal, Unmarshalⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(S.id, []); (ptrT.id S.id, [])].

#[global] Instance info' : PkgInfo appendargs_gk.appendargs_gk :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.github_com.tchajed.marshal.marshal; code.github_com.mit_pdos.gokv.reconfig.replica.logentry_gk.logentry_gk];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #appendargs_gk.appendargs_gk (λ: <>,
      exception_do (do:  (logentry_gk.initialize' #());;;
      do:  (marshal.initialize' #());;;
      do:  (package.alloc appendargs_gk.appendargs_gk #()))
      ).

End code.
End appendargs_gk.
