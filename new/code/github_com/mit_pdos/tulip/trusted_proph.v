(* autogenerated from github.com/mit-pdos/tulip/trusted_proph *)
From New.golang Require Import defn.

Definition trusted_proph : go_string := "github.com/mit-pdos/tulip/trusted_proph".

From New Require Import grove_prelude.
Module trusted_proph.
Section code.


Axiom ResolveRead : val.

Axiom ResolveAbort : val.

Axiom ResolveCommit : val.

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [("ResolveRead"%go, ResolveRead); ("ResolveAbort"%go, ResolveAbort); ("ResolveCommit"%go, ResolveCommit)].

Definition msets' : list (go_string * (list (go_string * val))) := [].

#[global] Instance info' : PkgInfo trusted_proph.trusted_proph :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [];
  |}.

Axiom _'init : val.

Definition initialize' : val :=
  rec: "initialize'" <> :=
    globals.package_init trusted_proph.trusted_proph (λ: <>,
      exception_do (do:  #())
      ).

End code.
End trusted_proph.
