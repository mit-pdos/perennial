(* autogenerated from github.com/mit-pdos/tulip/trusted_time *)
From New.golang Require Import defn.

Definition trusted_time : go_string := "github.com/mit-pdos/tulip/trusted_time".

Module trusted_time.
Section code.
Context `{ffi_syntax}.


Axiom GetTime : val.

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [("GetTime"%go, GetTime)].

Definition msets' : list (go_string * (list (go_string * val))) := [].

#[global] Instance info' : PkgInfo trusted_time.trusted_time :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [];
  |}.

Axiom _'init : val.

Definition initialize' : val :=
  rec: "initialize'" <> :=
    globals.package_init trusted_time.trusted_time (λ: <>,
      exception_do (do:  #())
      ).

End code.
End trusted_time.
