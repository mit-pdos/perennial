(* autogenerated from github.com/mit-pdos/tulip/txnlog *)
From New.golang Require Import defn.
Require Export New.code.github_com.mit_pdos.gokv.grove_ffi.
Require Export New.code.github_com.mit_pdos.tulip.paxos.
Require Export New.code.github_com.mit_pdos.tulip.tulip.
Require Export New.code.github_com.mit_pdos.tulip.util.
Require Export New.code.github_com.tchajed.marshal.

Definition txnlog : go_string := "github.com/mit-pdos/tulip/txnlog".

From New Require Import grove_prelude.
Module txnlog.
Section code.


Definition Cmd : go_type := structT [
  "Kind" :: uint64T;
  "Timestamp" :: uint64T;
  "PartialWrites" :: sliceT
].

Definition TxnLog : go_type := structT [
  "px" :: ptrT
].

Definition TXNLOG_ABORT : expr := #(W64 0).

Definition TXNLOG_COMMIT : expr := #(W64 1).

(* Arguments:
   @ts: Transaction timestamp.

   Return values:
   @lsn (uint64): @lsn = 0 indicates failure; otherwise, this indicates the
   logical index this command is supposed to be placed at.

   @term (uint64): The @term this command is supposed to have.

   Notes:
   1) Passing @lsn and @term to @WaitUntilSafe allows us to determine whether
   the command we just submitted actually get safely replicated (and wait until
   that happens up to some timeout).

   2) Although @term is redundant in that we can always detect failure by
   comparing the content of the command to some application state (e.g., a reply
   table), it can be seen as a performance optimization that would allow us to
   know earlier that our command has not been safely replicated (and hence
   resubmit). Another upside of having it would be that this allows the check to
   be done in a general way, without relying on the content.

   go: txnlog.go:49:20 *)
Definition TxnLog__SubmitCommit : val :=
  rec: "TxnLog__SubmitCommit" "log" "ts" "pwrs" :=
    exception_do (let: "log" := (mem.alloc "log") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ts" := (mem.alloc "ts") in
    let: "bs" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #byteT #(W64 0) #(W64 64)) in
    do:  ("bs" <-[#sliceT] "$r0");;;
    let: "bs1" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "bs") in
    let: "$a1" := TXNLOG_COMMIT in
    (func_call #marshal.marshal #"WriteInt"%go) "$a0" "$a1") in
    do:  ("bs1" <-[#sliceT] "$r0");;;
    let: "bs2" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "bs1") in
    let: "$a1" := (![#uint64T] "ts") in
    (func_call #marshal.marshal #"WriteInt"%go) "$a0" "$a1") in
    do:  ("bs2" <-[#sliceT] "$r0");;;
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "bs2") in
    let: "$a1" := (![#sliceT] "pwrs") in
    (func_call #util.util #"EncodeKVMapFromSlice"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    let: "term" := (mem.alloc (type.zero_val #uint64T)) in
    let: "lsn" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (string.from_bytes (![#sliceT] "data")) in
    (method_call #paxos #"Paxos'ptr" #"Submit" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("lsn" <-[#uint64T] "$r0");;;
    do:  ("term" <-[#uint64T] "$r1");;;
    return: (![#uint64T] "lsn", ![#uint64T] "term")).

(* Arguments and return values: see description of @SubmitPrepare.

   go: txnlog.go:61:20 *)
Definition TxnLog__SubmitAbort : val :=
  rec: "TxnLog__SubmitAbort" "log" "ts" :=
    exception_do (let: "log" := (mem.alloc "log") in
    let: "ts" := (mem.alloc "ts") in
    let: "bs" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make3 #byteT #(W64 0) #(W64 8)) in
    do:  ("bs" <-[#sliceT] "$r0");;;
    let: "bs1" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "bs") in
    let: "$a1" := TXNLOG_ABORT in
    (func_call #marshal.marshal #"WriteInt"%go) "$a0" "$a1") in
    do:  ("bs1" <-[#sliceT] "$r0");;;
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#sliceT] "bs1") in
    let: "$a1" := (![#uint64T] "ts") in
    (func_call #marshal.marshal #"WriteInt"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    let: "term" := (mem.alloc (type.zero_val #uint64T)) in
    let: "lsn" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (string.from_bytes (![#sliceT] "data")) in
    (method_call #paxos #"Paxos'ptr" #"Submit" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("lsn" <-[#uint64T] "$r0");;;
    do:  ("term" <-[#uint64T] "$r1");;;
    return: (![#uint64T] "lsn", ![#uint64T] "term")).

(* Arguments:
   @lsn: LSN of the command whose replication to wait for.

   @term: Expected term of the command.

   Return values:
   @replicated (bool): If @true, then the command at @lsn has term @term;
   otherwise, we know nothing, but the upper layer is suggested to resubmit the
   command.

   TODO: maybe this is a bad interface since now the users would have to make
   another call.

   go: txnlog.go:83:20 *)
Definition TxnLog__WaitUntilSafe : val :=
  rec: "TxnLog__WaitUntilSafe" "log" "lsn" "term" :=
    exception_do (let: "log" := (mem.alloc "log") in
    let: "term" := (mem.alloc "term") in
    let: "lsn" := (mem.alloc "lsn") in
    return: (let: "$a0" := (![#uint64T] "lsn") in
     let: "$a1" := (![#uint64T] "term") in
     (method_call #paxos #"Paxos'ptr" #"WaitUntilSafe" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) "$a0" "$a1")).

(* Argument:
   @lsn: Logical index of the queried command.

   go: txnlog.go:89:20 *)
Definition TxnLog__Lookup : val :=
  rec: "TxnLog__Lookup" "log" "lsn" :=
    exception_do (let: "log" := (mem.alloc "log") in
    let: "lsn" := (mem.alloc "lsn") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "s" := (mem.alloc (type.zero_val #stringT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "lsn") in
    (method_call #paxos #"Paxos'ptr" #"Lookup" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("s" <-[#stringT] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      return: (struct.make #Cmd [{
         "Kind" ::= type.zero_val #uint64T;
         "Timestamp" ::= type.zero_val #uint64T;
         "PartialWrites" ::= type.zero_val #sliceT
       }], #false)
    else do:  #());;;
    let: "bs" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (string.to_bytes (![#stringT] "s")) in
    do:  ("bs" <-[#sliceT] "$r0");;;
    let: "bs1" := (mem.alloc (type.zero_val #sliceT)) in
    let: "kind" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "bs") in
    (func_call #marshal.marshal #"ReadInt"%go) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("kind" <-[#uint64T] "$r0");;;
    do:  ("bs1" <-[#sliceT] "$r1");;;
    (if: (![#uint64T] "kind") = TXNLOG_COMMIT
    then
      let: "bs2" := (mem.alloc (type.zero_val #sliceT)) in
      let: "ts" := (mem.alloc (type.zero_val #uint64T)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "bs1") in
      (func_call #marshal.marshal #"ReadInt"%go) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("ts" <-[#uint64T] "$r0");;;
      do:  ("bs2" <-[#sliceT] "$r1");;;
      let: "pwrs" := (mem.alloc (type.zero_val #sliceT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "bs2") in
      (func_call #util.util #"DecodeKVMapIntoSlice"%go) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("pwrs" <-[#sliceT] "$r0");;;
      do:  "$r1";;;
      let: "cmd" := (mem.alloc (type.zero_val #Cmd)) in
      let: "$r0" := (let: "$Kind" := TXNLOG_COMMIT in
      let: "$Timestamp" := (![#uint64T] "ts") in
      let: "$PartialWrites" := (![#sliceT] "pwrs") in
      struct.make #Cmd [{
        "Kind" ::= "$Kind";
        "Timestamp" ::= "$Timestamp";
        "PartialWrites" ::= "$PartialWrites"
      }]) in
      do:  ("cmd" <-[#Cmd] "$r0");;;
      return: (![#Cmd] "cmd", #true)
    else do:  #());;;
    (if: (![#uint64T] "kind") = TXNLOG_ABORT
    then
      let: "ts" := (mem.alloc (type.zero_val #uint64T)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#sliceT] "bs1") in
      (func_call #marshal.marshal #"ReadInt"%go) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("ts" <-[#uint64T] "$r0");;;
      do:  "$r1";;;
      let: "cmd" := (mem.alloc (type.zero_val #Cmd)) in
      let: "$r0" := (let: "$Kind" := TXNLOG_ABORT in
      let: "$Timestamp" := (![#uint64T] "ts") in
      struct.make #Cmd [{
        "Kind" ::= "$Kind";
        "Timestamp" ::= "$Timestamp";
        "PartialWrites" ::= type.zero_val #sliceT
      }]) in
      do:  ("cmd" <-[#Cmd] "$r0");;;
      return: (![#Cmd] "cmd", #true)
    else do:  #());;;
    return: (struct.make #Cmd [{
       "Kind" ::= type.zero_val #uint64T;
       "Timestamp" ::= type.zero_val #uint64T;
       "PartialWrites" ::= type.zero_val #sliceT
     }], #false)).

(* go: txnlog.go:120:20 *)
Definition TxnLog__DumpState : val :=
  rec: "TxnLog__DumpState" "log" <> :=
    exception_do (let: "log" := (mem.alloc "log") in
    do:  ((method_call #paxos #"Paxos'ptr" #"DumpState" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) #())).

(* go: txnlog.go:124:20 *)
Definition TxnLog__ForceElection : val :=
  rec: "TxnLog__ForceElection" "log" <> :=
    exception_do (let: "log" := (mem.alloc "log") in
    do:  ((method_call #paxos #"Paxos'ptr" #"ForceElection" (![#ptrT] (struct.field_ref #TxnLog #"px"%go (![#ptrT] "log")))) #())).

(* go: txnlog.go:128:6 *)
Definition Start : val :=
  rec: "Start" "nidme" "addrm" "fname" :=
    exception_do (let: "fname" := (mem.alloc "fname") in
    let: "addrm" := (mem.alloc "addrm") in
    let: "nidme" := (mem.alloc "nidme") in
    let: "px" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "nidme") in
    let: "$a1" := (![#(mapT uint64T uint64T)] "addrm") in
    let: "$a2" := (![#stringT] "fname") in
    (func_call #paxos.paxos #"Start"%go) "$a0" "$a1" "$a2") in
    do:  ("px" <-[#ptrT] "$r0");;;
    let: "txnlog" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (mem.alloc (let: "$px" := (![#ptrT] "px") in
    struct.make #TxnLog [{
      "px" ::= "$px"
    }])) in
    do:  ("txnlog" <-[#ptrT] "$r0");;;
    return: (![#ptrT] "txnlog")).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [("Start"%go, Start)].

Definition msets' : list (go_string * (list (go_string * val))) := [("Cmd"%go, []); ("Cmd'ptr"%go, []); ("TxnLog"%go, []); ("TxnLog'ptr"%go, [("DumpState"%go, TxnLog__DumpState); ("ForceElection"%go, TxnLog__ForceElection); ("Lookup"%go, TxnLog__Lookup); ("SubmitAbort"%go, TxnLog__SubmitAbort); ("SubmitCommit"%go, TxnLog__SubmitCommit); ("WaitUntilSafe"%go, TxnLog__WaitUntilSafe)])].

#[global] Instance info' : PkgInfo txnlog.txnlog :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [grove_ffi.grove_ffi; marshal.marshal; tulip.tulip; paxos.paxos; util.util];
  |}.

Definition initialize' : val :=
  rec: "initialize'" <> :=
    globals.package_init txnlog.txnlog (λ: <>,
      exception_do (do:  util.initialize';;;
      do:  paxos.initialize';;;
      do:  tulip.initialize';;;
      do:  marshal.initialize';;;
      do:  grove_ffi.initialize')
      ).

End code.
End txnlog.
