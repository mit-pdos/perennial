(* autogenerated from github.com/mit-pdos/tulip/gcoord *)
From New.golang Require Import defn.
Require Export New.code.github_com.goose_lang.primitive.
Require Export New.code.github_com.mit_pdos.gokv.grove_ffi.
Require Export New.code.github_com.mit_pdos.tulip.message.
Require Export New.code.github_com.mit_pdos.tulip.params.
Require Export New.code.github_com.mit_pdos.tulip.quorum.
Require Export New.code.github_com.mit_pdos.tulip.tulip.
Require Export New.code.github_com.mit_pdos.tulip.util.
Require Export New.code.sync.

Definition gcoord : go_string := "github.com/mit-pdos/tulip/gcoord".

From New Require Import grove_prelude.
Module gcoord.
Section code.


Definition GroupCoordinator : go_type := structT [
  "rps" :: sliceT;
  "addrm" :: mapT uint64T uint64T;
  "mu" :: ptrT;
  "cv" :: ptrT;
  "cvrs" :: ptrT;
  "ts" :: uint64T;
  "idxleader" :: uint64T;
  "grd" :: ptrT;
  "gpp" :: ptrT;
  "tsfinals" :: mapT uint64T boolT;
  "conns" :: mapT uint64T grove_ffi.Connection
].

(* go: gcoord.go:49:6 *)
Definition Start : val :=
  rec: "Start" "addrm" :=
    exception_do (let: "addrm" := (mem.alloc "addrm") in
    let: "gcoord" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (![#(mapT uint64T uint64T)] "addrm") in
    (func_call #gcoord.gcoord #"mkGroupCoordinator"%go) "$a0") in
    do:  ("gcoord" <-[#ptrT] "$r0");;;
    let: "$range" := (![#(mapT uint64T uint64T)] "addrm") in
    (let: "ridloop" := (mem.alloc (type.zero_val #uint64T)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("ridloop" <-[#uint64T] "$key");;;
      let: "rid" := (mem.alloc (type.zero_val #uint64T)) in
      let: "$r0" := (![#uint64T] "ridloop") in
      do:  ("rid" <-[#uint64T] "$r0");;;
      let: "$go" := (λ: <>,
        exception_do (do:  (let: "$a0" := (![#uint64T] "rid") in
        (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ResponseSession" (![#ptrT] "gcoord")) "$a0"))
        ) in
      do:  (Fork ("$go" #()))));;;
    let: "$go" := (λ: <>,
      exception_do (do:  ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ResendSession" (![#ptrT] "gcoord")) #()))
      ) in
    do:  (Fork ("$go" #()));;;
    return: (![#ptrT] "gcoord")).

(* go: gcoord.go:67:6 *)
Definition mkGroupCoordinator : val :=
  rec: "mkGroupCoordinator" "addrm" :=
    exception_do (let: "addrm" := (mem.alloc "addrm") in
    let: "mu" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (mem.alloc (type.zero_val #sync.Mutex)) in
    do:  ("mu" <-[#ptrT] "$r0");;;
    let: "cv" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (interface.make #sync #"Mutex'ptr" (![#ptrT] "mu")) in
    (func_call #sync.sync #"NewCond"%go) "$a0") in
    do:  ("cv" <-[#ptrT] "$r0");;;
    let: "cvrs" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (interface.make #sync #"Mutex'ptr" (![#ptrT] "mu")) in
    (func_call #sync.sync #"NewCond"%go) "$a0") in
    do:  ("cvrs" <-[#ptrT] "$r0");;;
    let: "nrps" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T uint64T)] "addrm") in
    map.len "$a0")) in
    do:  ("nrps" <-[#uint64T] "$r0");;;
    let: "rps" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (slice.make2 #uint64T #(W64 0)) in
    do:  ("rps" <-[#sliceT] "$r0");;;
    let: "$range" := (![#(mapT uint64T uint64T)] "addrm") in
    (let: "rid" := (mem.alloc (type.zero_val #uint64T)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("rid" <-[#uint64T] "$key");;;
      let: "$r0" := (let: "$a0" := (![#sliceT] "rps") in
      let: "$a1" := ((let: "$sl0" := (![#uint64T] "rid") in
      slice.literal #uint64T ["$sl0"])) in
      (slice.append #uint64T) "$a0" "$a1") in
      do:  ("rps" <-[#sliceT] "$r0")));;;
    let: "gcoord" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (mem.alloc (let: "$rps" := (![#sliceT] "rps") in
    let: "$addrm" := (![#(mapT uint64T uint64T)] "addrm") in
    let: "$mu" := (![#ptrT] "mu") in
    let: "$cv" := (![#ptrT] "cv") in
    let: "$cvrs" := (![#ptrT] "cvrs") in
    let: "$idxleader" := #(W64 0) in
    let: "$grd" := (let: "$a0" := (![#uint64T] "nrps") in
    (func_call #gcoord.gcoord #"mkGroupReader"%go) "$a0") in
    let: "$gpp" := (let: "$a0" := (![#uint64T] "nrps") in
    (func_call #gcoord.gcoord #"mkGroupPreparer"%go) "$a0") in
    let: "$tsfinals" := (map.make #uint64T #boolT) in
    let: "$conns" := (map.make #uint64T #grove_ffi.Connection) in
    struct.make #GroupCoordinator [{
      "rps" ::= "$rps";
      "addrm" ::= "$addrm";
      "mu" ::= "$mu";
      "cv" ::= "$cv";
      "cvrs" ::= "$cvrs";
      "ts" ::= type.zero_val #uint64T;
      "idxleader" ::= "$idxleader";
      "grd" ::= "$grd";
      "gpp" ::= "$gpp";
      "tsfinals" ::= "$tsfinals";
      "conns" ::= "$conns"
    }])) in
    do:  ("gcoord" <-[#ptrT] "$r0");;;
    return: (![#ptrT] "gcoord")).

(* Arguments:
   @ts: Timestamp of the transaction performing this read.
   @key: Key to be read.

   Return value:
   @value: Value of @key.

   @gcoord.Read blocks until the value of @key is determined.

   go: gcoord.go:103:33 *)
Definition GroupCoordinator__Read : val :=
  rec: "GroupCoordinator__Read" "gcoord" "ts" "key" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "key" := (mem.alloc "key") in
    let: "ts" := (mem.alloc "ts") in
    let: "$range" := (![#(mapT uint64T uint64T)] (struct.field_ref #GroupCoordinator #"addrm"%go (![#ptrT] "gcoord"))) in
    (let: "ridloop" := (mem.alloc (type.zero_val #uint64T)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("ridloop" <-[#uint64T] "$key");;;
      let: "rid" := (mem.alloc (type.zero_val #uint64T)) in
      let: "$r0" := (![#uint64T] "ridloop") in
      do:  ("rid" <-[#uint64T] "$r0");;;
      let: "$go" := (λ: <>,
        exception_do (do:  (let: "$a0" := (![#uint64T] "rid") in
        let: "$a1" := (![#uint64T] "ts") in
        let: "$a2" := (![#stringT] "key") in
        (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ReadSession" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2"))
        ) in
      do:  (Fork ("$go" #()))));;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "v" := (mem.alloc (type.zero_val #tulip.Value)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := (![#stringT] "key") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"WaitUntilValueReady" (![#ptrT] "gcoord")) "$a0" "$a1") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("v" <-[#tulip.Value] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    return: (![#tulip.Value] "v", ![#boolT] "ok")).

(* go: gcoord.go:116:33 *)
Definition GroupCoordinator__ReadSession : val :=
  rec: "GroupCoordinator__ReadSession" "gcoord" "rid" "ts" "key" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "key" := (mem.alloc "key") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    (for: (λ: <>, (~ (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#stringT] "key") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ValueResponded" (![#ptrT] "gcoord")) "$a0" "$a1")) && (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"AttachedWith" (![#ptrT] "gcoord")) "$a0")); (λ: <>, Skip) := λ: <>,
      do:  (let: "$a0" := (![#uint64T] "rid") in
      let: "$a1" := (![#uint64T] "ts") in
      let: "$a2" := (![#stringT] "key") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendRead" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2");;;
      do:  (let: "$a0" := params.NS_RESEND_READ in
      (func_call #primitive.primitive #"Sleep"%go) "$a0"))).

Definition GroupReader : go_type := structT [
  "nrps" :: uint64T;
  "valuem" :: mapT stringT tulip.Value;
  "qreadm" :: mapT stringT (mapT uint64T tulip.Version)
].

(* go: gcoord.go:127:33 *)
Definition GroupCoordinator__WaitUntilValueReady : val :=
  rec: "GroupCoordinator__WaitUntilValueReady" "gcoord" "ts" "key" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "key" := (mem.alloc "key") in
    let: "ts" := (mem.alloc "ts") in
    let: "value" := (mem.alloc (type.zero_val #tulip.Value)) in
    let: "valid" := (mem.alloc (type.zero_val #boolT)) in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    (for: (λ: <>, #true); (λ: <>, Skip) := λ: <>,
      (if: (~ (let: "$a0" := (![#uint64T] "ts") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"attachedWith" (![#ptrT] "gcoord")) "$a0"))
      then
        let: "$r0" := #false in
        do:  ("valid" <-[#boolT] "$r0");;;
        break: #()
      else do:  #());;;
      let: "ok" := (mem.alloc (type.zero_val #boolT)) in
      let: "v" := (mem.alloc (type.zero_val #tulip.Value)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#stringT] "key") in
      (method_call #gcoord.gcoord #"GroupReader'ptr" #"read" (![#ptrT] (struct.field_ref #GroupCoordinator #"grd"%go (![#ptrT] "gcoord")))) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("v" <-[#tulip.Value] "$r0");;;
      do:  ("ok" <-[#boolT] "$r1");;;
      (if: ![#boolT] "ok"
      then
        let: "$r0" := (![#tulip.Value] "v") in
        do:  ("value" <-[#tulip.Value] "$r0");;;
        let: "$r0" := #true in
        do:  ("valid" <-[#boolT] "$r0");;;
        break: #()
      else do:  #());;;
      do:  ((method_call #sync #"Cond'ptr" #"Wait" (![#ptrT] (struct.field_ref #GroupCoordinator #"cv"%go (![#ptrT] "gcoord")))) #()));;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#tulip.Value] "value", ![#boolT] "valid")).

(* go: gcoord.go:155:33 *)
Definition GroupCoordinator__ValueResponded : val :=
  rec: "GroupCoordinator__ValueResponded" "gcoord" "rid" "key" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "key" := (mem.alloc "key") in
    let: "rid" := (mem.alloc "rid") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "done" := (mem.alloc (type.zero_val #boolT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#stringT] "key") in
    (method_call #gcoord.gcoord #"GroupReader'ptr" #"responded" (![#ptrT] (struct.field_ref #GroupCoordinator #"grd"%go (![#ptrT] "gcoord")))) "$a0" "$a1") in
    do:  ("done" <-[#boolT] "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#boolT] "done")).

(* Arguments:
   @ts: Transaction timestamp.

   Return values:
   @status: Transaction status.
   @valid: If true, the group coordinator is assigned the same timestamp @ts
   throughout the course of @Prepare. @status is meaningful iff @valid is true.

   @Prepare blocks until the prepare decision (one of prepared, committed,
   aborted) is made, or the associated timestamp has changed.

   go: gcoord.go:172:33 *)
Definition GroupCoordinator__Prepare : val :=
  rec: "GroupCoordinator__Prepare" "gcoord" "ts" "ptgs" "pwrs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ptgs" := (mem.alloc "ptgs") in
    let: "ts" := (mem.alloc "ts") in
    let: "$range" := (![#(mapT uint64T uint64T)] (struct.field_ref #GroupCoordinator #"addrm"%go (![#ptrT] "gcoord"))) in
    (let: "ridloop" := (mem.alloc (type.zero_val #uint64T)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("ridloop" <-[#uint64T] "$key");;;
      let: "rid" := (mem.alloc (type.zero_val #uint64T)) in
      let: "$r0" := (![#uint64T] "ridloop") in
      do:  ("rid" <-[#uint64T] "$r0");;;
      let: "$go" := (λ: <>,
        exception_do (do:  (let: "$a0" := (![#uint64T] "rid") in
        let: "$a1" := (![#uint64T] "ts") in
        let: "$a2" := (![#sliceT] "ptgs") in
        let: "$a3" := (![#tulip.KVMap] "pwrs") in
        (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"PrepareSession" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2" "$a3"))
        ) in
      do:  (Fork ("$go" #()))));;;
    let: "valid" := (mem.alloc (type.zero_val #boolT)) in
    let: "st" := (mem.alloc (type.zero_val #uint64T)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"WaitUntilPrepareDone" (![#ptrT] "gcoord")) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("st" <-[#uint64T] "$r0");;;
    do:  ("valid" <-[#boolT] "$r1");;;
    return: (![#uint64T] "st", ![#boolT] "valid")).

(* go: gcoord.go:185:33 *)
Definition GroupCoordinator__waitOnResendSignal : val :=
  rec: "GroupCoordinator__waitOnResendSignal" "gcoord" <> :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    do:  ((method_call #sync #"Cond'ptr" #"Wait" (![#ptrT] (struct.field_ref #GroupCoordinator #"cvrs"%go (![#ptrT] "gcoord")))) #());;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #())).

Definition GPP_REFRESH : expr := #(W64 5).

Definition GPP_QUERY : expr := #(W64 4).

Definition GPP_UNPREPARE : expr := #(W64 3).

Definition GPP_PREPARE : expr := #(W64 2).

Definition GPP_VALIDATE : expr := #(W64 1).

Definition GPP_FAST_PREPARE : expr := #(W64 0).

(* go: gcoord.go:191:33 *)
Definition GroupCoordinator__PrepareSession : val :=
  rec: "GroupCoordinator__PrepareSession" "gcoord" "rid" "ts" "ptgs" "pwrs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ptgs" := (mem.alloc "ptgs") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    (for: (λ: <>, #true); (λ: <>, Skip) := λ: <>,
      let: "attached" := (mem.alloc (type.zero_val #boolT)) in
      let: "act" := (mem.alloc (type.zero_val #uint64T)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "rid") in
      let: "$a1" := (![#uint64T] "ts") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"NextPrepareAction" (![#ptrT] "gcoord")) "$a0" "$a1") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("act" <-[#uint64T] "$r0");;;
      do:  ("attached" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "attached"))
      then break: #()
      else do:  #());;;
      (if: (![#uint64T] "act") = GPP_FAST_PREPARE
      then
        do:  (let: "$a0" := (![#uint64T] "rid") in
        let: "$a1" := (![#uint64T] "ts") in
        let: "$a2" := (![#(mapT stringT tulip.Value)] "pwrs") in
        let: "$a3" := (![#sliceT] "ptgs") in
        (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendFastPrepare" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2" "$a3")
      else
        (if: (![#uint64T] "act") = GPP_VALIDATE
        then
          do:  (let: "$a0" := (![#uint64T] "rid") in
          let: "$a1" := (![#uint64T] "ts") in
          let: "$a2" := (![#(mapT stringT tulip.Value)] "pwrs") in
          let: "$a3" := (![#sliceT] "ptgs") in
          (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendValidate" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2" "$a3")
        else
          (if: (![#uint64T] "act") = GPP_PREPARE
          then
            do:  (let: "$a0" := (![#uint64T] "rid") in
            let: "$a1" := (![#uint64T] "ts") in
            (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendPrepare" (![#ptrT] "gcoord")) "$a0" "$a1")
          else
            (if: (![#uint64T] "act") = GPP_UNPREPARE
            then
              do:  (let: "$a0" := (![#uint64T] "rid") in
              let: "$a1" := (![#uint64T] "ts") in
              (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendUnprepare" (![#ptrT] "gcoord")) "$a0" "$a1")
            else
              (if: (![#uint64T] "act") = GPP_QUERY
              then
                do:  (let: "$a0" := (![#uint64T] "rid") in
                let: "$a1" := (![#uint64T] "ts") in
                (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendQuery" (![#ptrT] "gcoord")) "$a0" "$a1")
              else
                (if: (![#uint64T] "act") = GPP_REFRESH
                then
                  do:  (let: "$a0" := (![#uint64T] "rid") in
                  let: "$a1" := (![#uint64T] "ts") in
                  (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendRefresh" (![#ptrT] "gcoord")) "$a0" "$a1")
                else do:  #()))))));;;
      (if: (![#uint64T] "act") = GPP_REFRESH
      then
        do:  (let: "$a0" := params.NS_SEND_REFRESH in
        (func_call #primitive.primitive #"Sleep"%go) "$a0")
      else do:  ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"waitOnResendSignal" (![#ptrT] "gcoord")) #())))).

Definition GPP_ABORTED : expr := #(W64 6).

Definition GPP_COMMITTED : expr := #(W64 5).

Definition GroupPreparer : go_type := structT [
  "nrps" :: uint64T;
  "phase" :: uint64T;
  "frespm" :: mapT uint64T boolT;
  "vdm" :: mapT uint64T boolT;
  "srespm" :: mapT uint64T boolT
].

(* go: gcoord.go:236:33 *)
Definition GroupCoordinator__WaitUntilPrepareDone : val :=
  rec: "GroupCoordinator__WaitUntilPrepareDone" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "phase" := (mem.alloc (type.zero_val #uint64T)) in
    let: "valid" := (mem.alloc (type.zero_val #boolT)) in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    (for: (λ: <>, #true); (λ: <>, Skip) := λ: <>,
      (if: (~ (let: "$a0" := (![#uint64T] "ts") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"attachedWith" (![#ptrT] "gcoord")) "$a0"))
      then
        let: "$r0" := #false in
        do:  ("valid" <-[#boolT] "$r0");;;
        break: #()
      else do:  #());;;
      let: "ready" := (mem.alloc (type.zero_val #boolT)) in
      let: "$r0" := ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"ready" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) #()) in
      do:  ("ready" <-[#boolT] "$r0");;;
      (if: ![#boolT] "ready"
      then
        let: "$r0" := ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"getPhase" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) #()) in
        do:  ("phase" <-[#uint64T] "$r0");;;
        let: "$r0" := #true in
        do:  ("valid" <-[#boolT] "$r0");;;
        break: #()
      else do:  #());;;
      do:  ((method_call #sync #"Cond'ptr" #"Wait" (![#ptrT] (struct.field_ref #GroupCoordinator #"cv"%go (![#ptrT] "gcoord")))) #()));;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    (if: (~ (![#boolT] "valid"))
    then return: (tulip.TXN_PREPARED, #false)
    else do:  #());;;
    (if: (![#uint64T] "phase") = GPP_COMMITTED
    then return: (tulip.TXN_COMMITTED, #true)
    else do:  #());;;
    (if: (![#uint64T] "phase") = GPP_ABORTED
    then return: (tulip.TXN_ABORTED, #true)
    else do:  #());;;
    return: (tulip.TXN_PREPARED, #true)).

(* go: gcoord.go:276:33 *)
Definition GroupCoordinator__NextPrepareAction : val :=
  rec: "GroupCoordinator__NextPrepareAction" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    (if: (~ (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"attachedWith" (![#ptrT] "gcoord")) "$a0"))
    then
      do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
      return: (#(W64 0), #false)
    else do:  #());;;
    let: "action" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "rid") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"action" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0") in
    do:  ("action" <-[#uint64T] "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#uint64T] "action", #true)).

(* go: gcoord.go:291:33 *)
Definition GroupCoordinator__Attach : val :=
  rec: "GroupCoordinator__Attach" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "$r0" := (![#uint64T] "ts") in
    do:  ((struct.field_ref #GroupCoordinator #"ts"%go (![#ptrT] "gcoord")) <-[#uint64T] "$r0");;;
    do:  ((method_call #gcoord.gcoord #"GroupReader'ptr" #"reset" (![#ptrT] (struct.field_ref #GroupCoordinator #"grd"%go (![#ptrT] "gcoord")))) #());;;
    do:  ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"attach" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) #());;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #())).

(* go: gcoord.go:299:33 *)
Definition GroupCoordinator__attachedWith : val :=
  rec: "GroupCoordinator__attachedWith" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    return: ((![#uint64T] (struct.field_ref #GroupCoordinator #"ts"%go (![#ptrT] "gcoord"))) = (![#uint64T] "ts"))).

(* go: gcoord.go:303:33 *)
Definition GroupCoordinator__AttachedWith : val :=
  rec: "GroupCoordinator__AttachedWith" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "b" := (mem.alloc (type.zero_val #boolT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"attachedWith" (![#ptrT] "gcoord")) "$a0") in
    do:  ("b" <-[#boolT] "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#boolT] "b")).

(* go: gcoord.go:310:33 *)
Definition GroupCoordinator__Commit : val :=
  rec: "GroupCoordinator__Commit" "gcoord" "ts" "pwrs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ts" := (mem.alloc "ts") in
    do:  (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"RegisterFinalization" (![#ptrT] "gcoord")) "$a0");;;
    let: "leader" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"GetLeader" (![#ptrT] "gcoord")) #()) in
    do:  ("leader" <-[#uint64T] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "leader") in
    let: "$a1" := (![#uint64T] "ts") in
    let: "$a2" := (![#tulip.KVMap] "pwrs") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendCommit" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2");;;
    do:  (let: "$a0" := params.NS_RESEND_COMMIT in
    (func_call #primitive.primitive #"Sleep"%go) "$a0");;;
    (for: (λ: <>, (~ (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Finalized" (![#ptrT] "gcoord")) "$a0"))); (λ: <>, Skip) := λ: <>,
      let: "$r0" := ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ChangeLeader" (![#ptrT] "gcoord")) #()) in
      do:  ("leader" <-[#uint64T] "$r0");;;
      do:  (let: "$a0" := (![#uint64T] "leader") in
      let: "$a1" := (![#uint64T] "ts") in
      let: "$a2" := (![#tulip.KVMap] "pwrs") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendCommit" (![#ptrT] "gcoord")) "$a0" "$a1" "$a2");;;
      do:  (let: "$a0" := params.NS_RESEND_COMMIT in
      (func_call #primitive.primitive #"Sleep"%go) "$a0"))).

(* go: gcoord.go:325:33 *)
Definition GroupCoordinator__Abort : val :=
  rec: "GroupCoordinator__Abort" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    do:  (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"RegisterFinalization" (![#ptrT] "gcoord")) "$a0");;;
    let: "leader" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"GetLeader" (![#ptrT] "gcoord")) #()) in
    do:  ("leader" <-[#uint64T] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "leader") in
    let: "$a1" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendAbort" (![#ptrT] "gcoord")) "$a0" "$a1");;;
    do:  (let: "$a0" := params.NS_RESEND_ABORT in
    (func_call #primitive.primitive #"Sleep"%go) "$a0");;;
    (for: (λ: <>, (~ (let: "$a0" := (![#uint64T] "ts") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Finalized" (![#ptrT] "gcoord")) "$a0"))); (λ: <>, Skip) := λ: <>,
      let: "$r0" := ((method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"ChangeLeader" (![#ptrT] "gcoord")) #()) in
      do:  ("leader" <-[#uint64T] "$r0");;;
      do:  (let: "$a0" := (![#uint64T] "leader") in
      let: "$a1" := (![#uint64T] "ts") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"SendAbort" (![#ptrT] "gcoord")) "$a0" "$a1");;;
      do:  (let: "$a0" := params.NS_RESEND_ABORT in
      (func_call #primitive.primitive #"Sleep"%go) "$a0"))).

(* go: gcoord.go:340:33 *)
Definition GroupCoordinator__RegisterFinalization : val :=
  rec: "GroupCoordinator__RegisterFinalization" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "$r0" := #true in
    do:  (map.insert (![#(mapT uint64T boolT)] (struct.field_ref #GroupCoordinator #"tsfinals"%go (![#ptrT] "gcoord"))) (![#uint64T] "ts") "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #())).

(* go: gcoord.go:346:33 *)
Definition GroupCoordinator__Finalized : val :=
  rec: "GroupCoordinator__Finalized" "gcoord" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T boolT)] (struct.field_ref #GroupCoordinator #"tsfinals"%go (![#ptrT] "gcoord"))) (![#uint64T] "ts")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  "$r0";;;
    do:  ("ok" <-[#boolT] "$r1");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: ((~ (![#boolT] "ok")))).

(* go: gcoord.go:353:33 *)
Definition GroupCoordinator__processFinalizationResult : val :=
  rec: "GroupCoordinator__processFinalizationResult" "gcoord" "ts" "res" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "res" := (mem.alloc "res") in
    let: "ts" := (mem.alloc "ts") in
    (if: (![#uint64T] "res") = tulip.REPLICA_WRONG_LEADER
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupCoordinator #"tsfinals"%go (![#ptrT] "gcoord"))) in
    let: "$a1" := (![#uint64T] "ts") in
    map.delete "$a0" "$a1")).

(* go: gcoord.go:360:33 *)
Definition GroupCoordinator__ChangeLeader : val :=
  rec: "GroupCoordinator__ChangeLeader" "gcoord" <> :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "idxleader" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (((![#uint64T] (struct.field_ref #GroupCoordinator #"idxleader"%go (![#ptrT] "gcoord"))) + #(W64 1)) `rem` (s_to_w64 (let: "$a0" := (![#sliceT] (struct.field_ref #GroupCoordinator #"rps"%go (![#ptrT] "gcoord"))) in
    slice.len "$a0"))) in
    do:  ("idxleader" <-[#uint64T] "$r0");;;
    let: "$r0" := (![#uint64T] "idxleader") in
    do:  ((struct.field_ref #GroupCoordinator #"idxleader"%go (![#ptrT] "gcoord")) <-[#uint64T] "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#uint64T] (slice.elem_ref #uint64T (![#sliceT] (struct.field_ref #GroupCoordinator #"rps"%go (![#ptrT] "gcoord"))) (![#uint64T] "idxleader")))).

(* go: gcoord.go:368:33 *)
Definition GroupCoordinator__GetLeader : val :=
  rec: "GroupCoordinator__GetLeader" "gcoord" <> :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "idxleader" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (![#uint64T] (struct.field_ref #GroupCoordinator #"idxleader"%go (![#ptrT] "gcoord"))) in
    do:  ("idxleader" <-[#uint64T] "$r0");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#uint64T] (slice.elem_ref #uint64T (![#sliceT] (struct.field_ref #GroupCoordinator #"rps"%go (![#ptrT] "gcoord"))) (![#uint64T] "idxleader")))).

(* go: gcoord.go:375:33 *)
Definition GroupCoordinator__ResendSession : val :=
  rec: "GroupCoordinator__ResendSession" "gcoord" <> :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    (for: (λ: <>, #true); (λ: <>, Skip) := λ: <>,
      do:  (let: "$a0" := params.NS_RESEND_PREPARE in
      (func_call #primitive.primitive #"Sleep"%go) "$a0");;;
      do:  ((method_call #sync #"Cond'ptr" #"Broadcast" (![#ptrT] (struct.field_ref #GroupCoordinator #"cvrs"%go (![#ptrT] "gcoord")))) #()))).

(* go: gcoord.go:383:33 *)
Definition GroupCoordinator__ResponseSession : val :=
  rec: "GroupCoordinator__ResponseSession" "gcoord" "rid" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "rid" := (mem.alloc "rid") in
    (for: (λ: <>, #true); (λ: <>, Skip) := λ: <>,
      let: "ok" := (mem.alloc (type.zero_val #boolT)) in
      let: "data" := (mem.alloc (type.zero_val #sliceT)) in
      let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "rid") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Receive" (![#ptrT] "gcoord")) "$a0") in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  ("data" <-[#sliceT] "$r0");;;
      do:  ("ok" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "ok"))
      then
        do:  (let: "$a0" := params.NS_RECONNECT in
        (func_call #primitive.primitive #"Sleep"%go) "$a0");;;
        continue: #()
      else do:  #());;;
      let: "msg" := (mem.alloc (type.zero_val #message.TxnResponse)) in
      let: "$r0" := (let: "$a0" := (![#sliceT] "data") in
      (func_call #message.message #"DecodeTxnResponse"%go) "$a0") in
      do:  ("msg" <-[#message.TxnResponse] "$r0");;;
      let: "kind" := (mem.alloc (type.zero_val #uint64T)) in
      let: "$r0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Kind"%go "msg")) in
      do:  ("kind" <-[#uint64T] "$r0");;;
      do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
      (if: ((![#uint64T] "kind") = message.MSG_TXN_COMMIT) || ((![#uint64T] "kind") = message.MSG_TXN_ABORT)
      then
        do:  (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Timestamp"%go "msg")) in
        let: "$a1" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
        (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"processFinalizationResult" (![#ptrT] "gcoord")) "$a0" "$a1");;;
        do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
        continue: #()
      else do:  #());;;
      (if: (~ (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Timestamp"%go "msg")) in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"attachedWith" (![#ptrT] "gcoord")) "$a0"))
      then
        do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
        continue: #()
      else do:  #());;;
      (if: (![#uint64T] "kind") = message.MSG_TXN_READ
      then
        do:  (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"ReplicaID"%go "msg")) in
        let: "$a1" := (![#stringT] (struct.field_ref #message.TxnResponse #"Key"%go "msg")) in
        let: "$a2" := (![#tulip.Version] (struct.field_ref #message.TxnResponse #"Version"%go "msg")) in
        let: "$a3" := (![#boolT] (struct.field_ref #message.TxnResponse #"Slow"%go "msg")) in
        (method_call #gcoord.gcoord #"GroupReader'ptr" #"processReadResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"grd"%go (![#ptrT] "gcoord")))) "$a0" "$a1" "$a2" "$a3")
      else
        (if: (![#uint64T] "kind") = message.MSG_TXN_FAST_PREPARE
        then
          let: "resend" := (mem.alloc (type.zero_val #boolT)) in
          let: "$r0" := (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"ReplicaID"%go "msg")) in
          let: "$a1" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
          (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"processFastPrepareResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0" "$a1") in
          do:  ("resend" <-[#boolT] "$r0");;;
          (if: ![#boolT] "resend"
          then do:  ((method_call #sync #"Cond'ptr" #"Broadcast" (![#ptrT] (struct.field_ref #GroupCoordinator #"cvrs"%go (![#ptrT] "gcoord")))) #())
          else do:  #())
        else
          (if: (![#uint64T] "kind") = message.MSG_TXN_VALIDATE
          then
            let: "resend" := (mem.alloc (type.zero_val #boolT)) in
            let: "$r0" := (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"ReplicaID"%go "msg")) in
            let: "$a1" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
            (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"processValidateResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0" "$a1") in
            do:  ("resend" <-[#boolT] "$r0");;;
            (if: ![#boolT] "resend"
            then do:  ((method_call #sync #"Cond'ptr" #"Broadcast" (![#ptrT] (struct.field_ref #GroupCoordinator #"cvrs"%go (![#ptrT] "gcoord")))) #())
            else do:  #())
          else
            (if: (![#uint64T] "kind") = message.MSG_TXN_PREPARE
            then
              (if: (![#uint64T] (struct.field_ref #message.TxnResponse #"Rank"%go "msg")) = #(W64 1)
              then
                do:  (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"ReplicaID"%go "msg")) in
                let: "$a1" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
                (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"processPrepareResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0" "$a1")
              else do:  #())
            else
              (if: (![#uint64T] "kind") = message.MSG_TXN_UNPREPARE
              then
                (if: (![#uint64T] (struct.field_ref #message.TxnResponse #"Rank"%go "msg")) = #(W64 1)
                then
                  do:  (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"ReplicaID"%go "msg")) in
                  let: "$a1" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
                  (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"processUnprepareResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0" "$a1")
                else do:  #())
              else
                (if: (![#uint64T] "kind") = message.MSG_TXN_QUERY
                then
                  do:  (let: "$a0" := (![#uint64T] (struct.field_ref #message.TxnResponse #"Result"%go "msg")) in
                  (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"processQueryResult" (![#ptrT] (struct.field_ref #GroupCoordinator #"gpp"%go (![#ptrT] "gcoord")))) "$a0")
                else
                  (if: (![#uint64T] "kind") = message.MSG_TXN_REFRESH
                  then do:  #()
                  else do:  #())))))));;;
      do:  ((method_call #sync #"Cond'ptr" #"Broadcast" (![#ptrT] (struct.field_ref #GroupCoordinator #"cv"%go (![#ptrT] "gcoord")))) #());;;
      do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #()))).

(* go: gcoord.go:452:33 *)
Definition GroupCoordinator__Send : val :=
  rec: "GroupCoordinator__Send" "gcoord" "rid" "data" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "data" := (mem.alloc "data") in
    let: "rid" := (mem.alloc "rid") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "conn" := (mem.alloc (type.zero_val #grove_ffi.Connection)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "rid") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"GetConnection" (![#ptrT] "gcoord")) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("conn" <-[#grove_ffi.Connection] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      do:  (let: "$a0" := (![#uint64T] "rid") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Connect" (![#ptrT] "gcoord")) "$a0");;;
      return: (#())
    else do:  #());;;
    let: "err" := (mem.alloc (type.zero_val #boolT)) in
    let: "$r0" := (let: "$a0" := (![#grove_ffi.Connection] "conn") in
    let: "$a1" := (![#sliceT] "data") in
    (func_call #grove_ffi.grove_ffi #"Send"%go) "$a0" "$a1") in
    do:  ("err" <-[#boolT] "$r0");;;
    (if: ![#boolT] "err"
    then
      do:  (let: "$a0" := (![#uint64T] "rid") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Connect" (![#ptrT] "gcoord")) "$a0")
    else do:  #())).

(* go: gcoord.go:465:33 *)
Definition GroupCoordinator__Receive : val :=
  rec: "GroupCoordinator__Receive" "gcoord" "rid" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "rid" := (mem.alloc "rid") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "conn" := (mem.alloc (type.zero_val #grove_ffi.Connection)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#uint64T] "rid") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"GetConnection" (![#ptrT] "gcoord")) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("conn" <-[#grove_ffi.Connection] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      do:  (let: "$a0" := (![#uint64T] "rid") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Connect" (![#ptrT] "gcoord")) "$a0");;;
      return: (#slice.nil, #false)
    else do:  #());;;
    let: "ret" := (mem.alloc (type.zero_val #grove_ffi.ReceiveRet)) in
    let: "$r0" := (let: "$a0" := (![#grove_ffi.Connection] "conn") in
    (func_call #grove_ffi.grove_ffi #"Receive"%go) "$a0") in
    do:  ("ret" <-[#grove_ffi.ReceiveRet] "$r0");;;
    (if: ![#boolT] (struct.field_ref #grove_ffi.ReceiveRet #"Err"%go "ret")
    then
      do:  (let: "$a0" := (![#uint64T] "rid") in
      (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Connect" (![#ptrT] "gcoord")) "$a0");;;
      return: (#slice.nil, #false)
    else do:  #());;;
    return: (![#sliceT] (struct.field_ref #grove_ffi.ReceiveRet #"Data"%go "ret"), #true)).

(* go: gcoord.go:481:33 *)
Definition GroupCoordinator__SendRead : val :=
  rec: "GroupCoordinator__SendRead" "gcoord" "rid" "ts" "key" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "key" := (mem.alloc "key") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := (![#stringT] "key") in
    (func_call #message.message #"EncodeTxnReadRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:486:33 *)
Definition GroupCoordinator__SendFastPrepare : val :=
  rec: "GroupCoordinator__SendFastPrepare" "gcoord" "rid" "ts" "pwrs" "ptgs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ptgs" := (mem.alloc "ptgs") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := (![#tulip.KVMap] "pwrs") in
    let: "$a2" := (![#sliceT] "ptgs") in
    (func_call #message.message #"EncodeTxnFastPrepareRequest"%go) "$a0" "$a1" "$a2") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:491:33 *)
Definition GroupCoordinator__SendValidate : val :=
  rec: "GroupCoordinator__SendValidate" "gcoord" "rid" "ts" "pwrs" "ptgs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ptgs" := (mem.alloc "ptgs") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := #(W64 1) in
    let: "$a2" := (![#tulip.KVMap] "pwrs") in
    let: "$a3" := (![#sliceT] "ptgs") in
    (func_call #message.message #"EncodeTxnValidateRequest"%go) "$a0" "$a1" "$a2" "$a3") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:496:33 *)
Definition GroupCoordinator__SendPrepare : val :=
  rec: "GroupCoordinator__SendPrepare" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := #(W64 1) in
    (func_call #message.message #"EncodeTxnPrepareRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:501:33 *)
Definition GroupCoordinator__SendUnprepare : val :=
  rec: "GroupCoordinator__SendUnprepare" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := #(W64 1) in
    (func_call #message.message #"EncodeTxnUnprepareRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:506:33 *)
Definition GroupCoordinator__SendQuery : val :=
  rec: "GroupCoordinator__SendQuery" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := #(W64 1) in
    (func_call #message.message #"EncodeTxnQueryRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:511:33 *)
Definition GroupCoordinator__SendRefresh : val :=
  rec: "GroupCoordinator__SendRefresh" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := #(W64 1) in
    (func_call #message.message #"EncodeTxnRefreshRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:516:33 *)
Definition GroupCoordinator__SendCommit : val :=
  rec: "GroupCoordinator__SendCommit" "gcoord" "rid" "ts" "pwrs" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "pwrs" := (mem.alloc "pwrs") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    let: "$a1" := (![#tulip.KVMap] "pwrs") in
    (func_call #message.message #"EncodeTxnCommitRequest"%go) "$a0" "$a1") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:521:33 *)
Definition GroupCoordinator__SendAbort : val :=
  rec: "GroupCoordinator__SendAbort" "gcoord" "rid" "ts" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "ts" := (mem.alloc "ts") in
    let: "rid" := (mem.alloc "rid") in
    let: "data" := (mem.alloc (type.zero_val #sliceT)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "ts") in
    (func_call #message.message #"EncodeTxnAbortRequest"%go) "$a0") in
    do:  ("data" <-[#sliceT] "$r0");;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := (![#sliceT] "data") in
    (method_call #gcoord.gcoord #"GroupCoordinator'ptr" #"Send" (![#ptrT] "gcoord")) "$a0" "$a1")).

(* go: gcoord.go:526:33 *)
Definition GroupCoordinator__GetConnection : val :=
  rec: "GroupCoordinator__GetConnection" "gcoord" "rid" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "rid" := (mem.alloc "rid") in
    do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "conn" := (mem.alloc (type.zero_val #grove_ffi.Connection)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T grove_ffi.Connection)] (struct.field_ref #GroupCoordinator #"conns"%go (![#ptrT] "gcoord"))) (![#uint64T] "rid")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("conn" <-[#grove_ffi.Connection] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
    return: (![#grove_ffi.Connection] "conn", ![#boolT] "ok")).

(* go: gcoord.go:533:33 *)
Definition GroupCoordinator__Connect : val :=
  rec: "GroupCoordinator__Connect" "gcoord" "rid" :=
    exception_do (let: "gcoord" := (mem.alloc "gcoord") in
    let: "rid" := (mem.alloc "rid") in
    let: "addr" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (Fst (map.get (![#(mapT uint64T uint64T)] (struct.field_ref #GroupCoordinator #"addrm"%go (![#ptrT] "gcoord"))) (![#uint64T] "rid"))) in
    do:  ("addr" <-[#uint64T] "$r0");;;
    let: "ret" := (mem.alloc (type.zero_val #grove_ffi.ConnectRet)) in
    let: "$r0" := (let: "$a0" := (![#uint64T] "addr") in
    (func_call #grove_ffi.grove_ffi #"Connect"%go) "$a0") in
    do:  ("ret" <-[#grove_ffi.ConnectRet] "$r0");;;
    (if: (~ (![#boolT] (struct.field_ref #grove_ffi.ConnectRet #"Err"%go "ret")))
    then
      do:  ((method_call #sync #"Mutex'ptr" #"Lock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
      let: "$r0" := (![#grove_ffi.Connection] (struct.field_ref #grove_ffi.ConnectRet #"Connection"%go "ret")) in
      do:  (map.insert (![#(mapT uint64T grove_ffi.Connection)] (struct.field_ref #GroupCoordinator #"conns"%go (![#ptrT] "gcoord"))) (![#uint64T] "rid") "$r0");;;
      do:  ((method_call #sync #"Mutex'ptr" #"Unlock" (![#ptrT] (struct.field_ref #GroupCoordinator #"mu"%go (![#ptrT] "gcoord")))) #());;;
      return: (#true)
    else do:  #());;;
    return: (#false)).

(* go: gcoord.go:560:6 *)
Definition mkGroupReader : val :=
  rec: "mkGroupReader" "nrps" :=
    exception_do (let: "nrps" := (mem.alloc "nrps") in
    let: "grd" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (mem.alloc (let: "$nrps" := (![#uint64T] "nrps") in
    struct.make #GroupReader [{
      "nrps" ::= "$nrps";
      "valuem" ::= type.zero_val #(mapT stringT tulip.Value);
      "qreadm" ::= type.zero_val #(mapT stringT (mapT uint64T tulip.Version))
    }])) in
    do:  ("grd" <-[#ptrT] "$r0");;;
    do:  ((method_call #gcoord.gcoord #"GroupReader'ptr" #"reset" (![#ptrT] "grd")) #());;;
    return: (![#ptrT] "grd")).

(* go: gcoord.go:567:25 *)
Definition GroupReader__reset : val :=
  rec: "GroupReader__reset" "grd" <> :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "$r0" := (map.make #stringT #tulip.Value) in
    do:  ((struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd")) <-[#(mapT stringT tulip.Value)] "$r0");;;
    let: "$r0" := (map.make #stringT #(mapT uint64T tulip.Version)) in
    do:  ((struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd")) <-[#(mapT stringT (mapT uint64T tulip.Version))] "$r0")).

(* go: gcoord.go:572:25 *)
Definition GroupReader__cquorum : val :=
  rec: "GroupReader__cquorum" "grd" "n" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "n" := (mem.alloc "n") in
    return: ((let: "$a0" := (![#uint64T] (struct.field_ref #GroupReader #"nrps"%go (![#ptrT] "grd"))) in
     (func_call #quorum.quorum #"ClassicQuorum"%go) "$a0") ≤ (![#uint64T] "n"))).

(* go: gcoord.go:576:25 *)
Definition GroupReader__pickLatestValue : val :=
  rec: "GroupReader__pickLatestValue" "grd" "key" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "key" := (mem.alloc "key") in
    let: "lts" := (mem.alloc (type.zero_val #uint64T)) in
    let: "value" := (mem.alloc (type.zero_val #tulip.Value)) in
    let: "verm" := (mem.alloc (type.zero_val #(mapT uint64T tulip.Version))) in
    let: "$r0" := (Fst (map.get (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) (![#stringT] "key"))) in
    do:  ("verm" <-[#(mapT uint64T tulip.Version)] "$r0");;;
    let: "$range" := (![#(mapT uint64T tulip.Version)] "verm") in
    (let: "ver" := (mem.alloc (type.zero_val #uint64T)) in
    map.for_range "$range" (λ: "$key" "value",
      do:  ("ver" <-[#tulip.Version] "$value");;;
      do:  "$key";;;
      (if: (![#uint64T] "lts") ≤ (![#uint64T] (struct.field_ref #tulip.Version #"Timestamp"%go "ver"))
      then
        let: "$r0" := (![#tulip.Value] (struct.field_ref #tulip.Version #"Value"%go "ver")) in
        do:  ("value" <-[#tulip.Value] "$r0");;;
        let: "$r0" := (![#uint64T] (struct.field_ref #tulip.Version #"Timestamp"%go "ver")) in
        do:  ("lts" <-[#uint64T] "$r0")
      else do:  #())));;;
    return: (![#tulip.Value] "value")).

(* go: gcoord.go:591:25 *)
Definition GroupReader__read : val :=
  rec: "GroupReader__read" "grd" "key" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "key" := (mem.alloc "key") in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "v" := (mem.alloc (type.zero_val #tulip.Value)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT stringT tulip.Value)] (struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd"))) (![#stringT] "key")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("v" <-[#tulip.Value] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    return: (![#tulip.Value] "v", ![#boolT] "ok")).

(* go: gcoord.go:596:25 *)
Definition GroupReader__responded : val :=
  rec: "GroupReader__responded" "grd" "rid" "key" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "key" := (mem.alloc "key") in
    let: "rid" := (mem.alloc "rid") in
    let: "final" := (mem.alloc (type.zero_val #boolT)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT stringT tulip.Value)] (struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd"))) (![#stringT] "key")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  "$r0";;;
    do:  ("final" <-[#boolT] "$r1");;;
    (if: ![#boolT] "final"
    then return: (#true)
    else do:  #());;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "qread" := (mem.alloc (type.zero_val #(mapT uint64T tulip.Version))) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) (![#stringT] "key")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("qread" <-[#(mapT uint64T tulip.Version)] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then return: (#false)
    else do:  #());;;
    let: "responded" := (mem.alloc (type.zero_val #boolT)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T tulip.Version)] "qread") (![#uint64T] "rid")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  "$r0";;;
    do:  ("responded" <-[#boolT] "$r1");;;
    (if: ![#boolT] "responded"
    then return: (#true)
    else do:  #());;;
    return: (#false)).

(* go: gcoord.go:617:25 *)
Definition GroupReader__clearVersions : val :=
  rec: "GroupReader__clearVersions" "grd" "key" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "key" := (mem.alloc "key") in
    do:  (let: "$a0" := (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) in
    let: "$a1" := (![#stringT] "key") in
    map.delete "$a0" "$a1")).

(* go: gcoord.go:621:25 *)
Definition GroupReader__processReadResult : val :=
  rec: "GroupReader__processReadResult" "grd" "rid" "key" "ver" "slow" :=
    exception_do (let: "grd" := (mem.alloc "grd") in
    let: "slow" := (mem.alloc "slow") in
    let: "ver" := (mem.alloc "ver") in
    let: "key" := (mem.alloc "key") in
    let: "rid" := (mem.alloc "rid") in
    let: "final" := (mem.alloc (type.zero_val #boolT)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT stringT tulip.Value)] (struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd"))) (![#stringT] "key")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  "$r0";;;
    do:  ("final" <-[#boolT] "$r1");;;
    (if: ![#boolT] "final"
    then return: (#())
    else do:  #());;;
    (if: (~ (![#boolT] "slow"))
    then
      let: "$r0" := (![#tulip.Value] (struct.field_ref #tulip.Version #"Value"%go "ver")) in
      do:  (map.insert (![#(mapT stringT tulip.Value)] (struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd"))) (![#stringT] "key") "$r0");;;
      do:  (let: "$a0" := (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) in
      let: "$a1" := (![#stringT] "key") in
      map.delete "$a0" "$a1");;;
      return: (#())
    else do:  #());;;
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "qread" := (mem.alloc (type.zero_val #(mapT uint64T tulip.Version))) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) (![#stringT] "key")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("qread" <-[#(mapT uint64T tulip.Version)] "$r0");;;
    do:  ("ok" <-[#boolT] "$r1");;;
    (if: (~ (![#boolT] "ok"))
    then
      let: "verm" := (mem.alloc (type.zero_val #(mapT uint64T tulip.Version))) in
      let: "$r0" := (map.make #uint64T #tulip.Version) in
      do:  ("verm" <-[#(mapT uint64T tulip.Version)] "$r0");;;
      let: "$r0" := (![#tulip.Version] "ver") in
      do:  (map.insert (![#(mapT uint64T tulip.Version)] "verm") (![#uint64T] "rid") "$r0");;;
      let: "$r0" := (![#(mapT uint64T tulip.Version)] "verm") in
      do:  (map.insert (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) (![#stringT] "key") "$r0");;;
      return: (#())
    else do:  #());;;
    let: "responded" := (mem.alloc (type.zero_val #boolT)) in
    let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T tulip.Version)] "qread") (![#uint64T] "rid")) in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  "$r0";;;
    do:  ("responded" <-[#boolT] "$r1");;;
    (if: ![#boolT] "responded"
    then return: (#())
    else do:  #());;;
    let: "$r0" := (![#tulip.Version] "ver") in
    do:  (map.insert (![#(mapT uint64T tulip.Version)] "qread") (![#uint64T] "rid") "$r0");;;
    let: "$r0" := (![#(mapT uint64T tulip.Version)] "qread") in
    do:  (map.insert (![#(mapT stringT (mapT uint64T tulip.Version))] (struct.field_ref #GroupReader #"qreadm"%go (![#ptrT] "grd"))) (![#stringT] "key") "$r0");;;
    let: "n" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T tulip.Version)] "qread") in
    map.len "$a0")) in
    do:  ("n" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "n") in
    (method_call #gcoord.gcoord #"GroupReader'ptr" #"cquorum" (![#ptrT] "grd")) "$a0"))
    then return: (#())
    else do:  #());;;
    let: "latest" := (mem.alloc (type.zero_val #tulip.Value)) in
    let: "$r0" := (let: "$a0" := (![#stringT] "key") in
    (method_call #gcoord.gcoord #"GroupReader'ptr" #"pickLatestValue" (![#ptrT] "grd")) "$a0") in
    do:  ("latest" <-[#tulip.Value] "$r0");;;
    let: "$r0" := (![#tulip.Value] "latest") in
    do:  (map.insert (![#(mapT stringT tulip.Value)] (struct.field_ref #GroupReader #"valuem"%go (![#ptrT] "grd"))) (![#stringT] "key") "$r0");;;
    do:  (let: "$a0" := (![#stringT] "key") in
    (method_call #gcoord.gcoord #"GroupReader'ptr" #"clearVersions" (![#ptrT] "grd")) "$a0")).

Definition GPP_WAITING : expr := #(W64 3).

(* go: gcoord.go:696:6 *)
Definition mkGroupPreparer : val :=
  rec: "mkGroupPreparer" "nrps" :=
    exception_do (let: "nrps" := (mem.alloc "nrps") in
    let: "gpp" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (mem.alloc (let: "$nrps" := (![#uint64T] "nrps") in
    struct.make #GroupPreparer [{
      "nrps" ::= "$nrps";
      "phase" ::= type.zero_val #uint64T;
      "frespm" ::= type.zero_val #(mapT uint64T boolT);
      "vdm" ::= type.zero_val #(mapT uint64T boolT);
      "srespm" ::= type.zero_val #(mapT uint64T boolT)
    }])) in
    do:  ("gpp" <-[#ptrT] "$r0");;;
    let: "$r0" := GPP_WAITING in
    do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0");;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"vdm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0");;;
    return: (![#ptrT] "gpp")).

Definition GPP_VALIDATING : expr := #(W64 0).

(* go: gcoord.go:705:27 *)
Definition GroupPreparer__attach : val :=
  rec: "GroupPreparer__attach" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "$r0" := GPP_VALIDATING in
    do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0");;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"vdm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0")).

Definition GPP_PREPARING : expr := #(W64 1).

Definition GPP_UNPREPARING : expr := #(W64 2).

Definition GPP_PREPARED : expr := #(W64 4).

(* Argument:
   @rid: ID of the replica to which a new action is performed.

   Return value:
   @action: Next action to perform.

   go: gcoord.go:737:27 *)
Definition GroupPreparer__action : val :=
  rec: "GroupPreparer__action" "gpp" "rid" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "rid" := (mem.alloc "rid") in
    (if: let: "$a0" := GPP_VALIDATING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"
    then
      let: "fresp" := (mem.alloc (type.zero_val #boolT)) in
      let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid")) in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  "$r0";;;
      do:  ("fresp" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "fresp"))
      then return: (GPP_FAST_PREPARE)
      else do:  #());;;
      let: "validated" := (mem.alloc (type.zero_val #boolT)) in
      let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"vdm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid")) in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  "$r0";;;
      do:  ("validated" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "validated"))
      then return: (GPP_VALIDATE)
      else do:  #());;;
      return: (GPP_QUERY)
    else do:  #());;;
    (if: let: "$a0" := GPP_PREPARING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"
    then
      let: "prepared" := (mem.alloc (type.zero_val #boolT)) in
      let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid")) in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  "$r0";;;
      do:  ("prepared" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "prepared"))
      then return: (GPP_PREPARE)
      else do:  #());;;
      return: (GPP_QUERY)
    else do:  #());;;
    (if: let: "$a0" := GPP_UNPREPARING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"
    then
      let: "unprepared" := (mem.alloc (type.zero_val #boolT)) in
      let: ("$ret0", "$ret1") := (map.get (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid")) in
      let: "$r0" := "$ret0" in
      let: "$r1" := "$ret1" in
      do:  "$r0";;;
      do:  ("unprepared" <-[#boolT] "$r1");;;
      (if: (~ (![#boolT] "unprepared"))
      then return: (GPP_UNPREPARE)
      else do:  #());;;
      return: (GPP_QUERY)
    else do:  #());;;
    (if: let: "$a0" := GPP_WAITING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"
    then return: (GPP_QUERY)
    else do:  #());;;
    return: (GPP_REFRESH)).

(* go: gcoord.go:785:27 *)
Definition GroupPreparer__fquorum : val :=
  rec: "GroupPreparer__fquorum" "gpp" "n" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "n" := (mem.alloc "n") in
    return: ((let: "$a0" := (![#uint64T] (struct.field_ref #GroupPreparer #"nrps"%go (![#ptrT] "gpp"))) in
     (func_call #quorum.quorum #"FastQuorum"%go) "$a0") ≤ (![#uint64T] "n"))).

(* go: gcoord.go:789:27 *)
Definition GroupPreparer__cquorum : val :=
  rec: "GroupPreparer__cquorum" "gpp" "n" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "n" := (mem.alloc "n") in
    return: ((let: "$a0" := (![#uint64T] (struct.field_ref #GroupPreparer #"nrps"%go (![#ptrT] "gpp"))) in
     (func_call #quorum.quorum #"ClassicQuorum"%go) "$a0") ≤ (![#uint64T] "n"))).

(* go: gcoord.go:793:27 *)
Definition GroupPreparer__hcquorum : val :=
  rec: "GroupPreparer__hcquorum" "gpp" "n" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "n" := (mem.alloc "n") in
    return: ((let: "$a0" := (let: "$a0" := (![#uint64T] (struct.field_ref #GroupPreparer #"nrps"%go (![#ptrT] "gpp"))) in
     (func_call #quorum.quorum #"ClassicQuorum"%go) "$a0") in
     (func_call #quorum.quorum #"Half"%go) "$a0") ≤ (![#uint64T] "n"))).

(* go: gcoord.go:797:27 *)
Definition GroupPreparer__ready : val :=
  rec: "GroupPreparer__ready" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    return: (GPP_PREPARED ≤ (![#uint64T] (struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp"))))).

(* go: gcoord.go:801:27 *)
Definition GroupPreparer__tryResign : val :=
  rec: "GroupPreparer__tryResign" "gpp" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    (if: (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"ready" (![#ptrT] "gpp")) #()
    then return: (#true)
    else do:  #());;;
    (if: (![#uint64T] "res") = tulip.REPLICA_COMMITTED_TXN
    then
      let: "$r0" := GPP_COMMITTED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
      return: (#true)
    else do:  #());;;
    (if: (![#uint64T] "res") = tulip.REPLICA_ABORTED_TXN
    then
      let: "$r0" := GPP_ABORTED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
      return: (#true)
    else do:  #());;;
    (if: (![#uint64T] "res") = tulip.REPLICA_STALE_COORDINATOR
    then return: (#true)
    else do:  #());;;
    return: (#false)).

(* go: gcoord.go:824:27 *)
Definition GroupPreparer__tryFastAbort : val :=
  rec: "GroupPreparer__tryFastAbort" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "n" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    let: "$a1" := #false in
    (func_call #util.util #"CountBoolMap"%go) "$a0" "$a1") in
    do:  ("n" <-[#uint64T] "$r0");;;
    (if: let: "$a0" := (![#uint64T] "n") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"fquorum" (![#ptrT] "gpp")) "$a0"
    then
      let: "$r0" := GPP_ABORTED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
      return: (#true)
    else do:  #());;;
    return: (#false)).

(* go: gcoord.go:836:27 *)
Definition GroupPreparer__tryFastPrepare : val :=
  rec: "GroupPreparer__tryFastPrepare" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "n" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    let: "$a1" := #true in
    (func_call #util.util #"CountBoolMap"%go) "$a0" "$a1") in
    do:  ("n" <-[#uint64T] "$r0");;;
    (if: let: "$a0" := (![#uint64T] "n") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"fquorum" (![#ptrT] "gpp")) "$a0"
    then
      let: "$r0" := GPP_PREPARED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
      return: (#true)
    else do:  #());;;
    return: (#false)).

(* go: gcoord.go:848:27 *)
Definition GroupPreparer__tryBecomePreparing : val :=
  rec: "GroupPreparer__tryBecomePreparing" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "nvd" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"vdm"%go (![#ptrT] "gpp"))) in
    map.len "$a0")) in
    do:  ("nvd" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "nvd") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"cquorum" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    let: "nresp" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    map.len "$a0")) in
    do:  ("nresp" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "nresp") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"cquorum" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    let: "nfp" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    let: "$a1" := #true in
    (func_call #util.util #"CountBoolMap"%go) "$a0" "$a1") in
    do:  ("nfp" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "nfp") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"hcquorum" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0");;;
    let: "$r0" := GPP_PREPARING in
    do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
    return: (#true)).

(* go: gcoord.go:878:27 *)
Definition GroupPreparer__tryBecomeUnpreparing : val :=
  rec: "GroupPreparer__tryBecomeUnpreparing" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "nresp" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    map.len "$a0")) in
    do:  ("nresp" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "nresp") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"cquorum" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    let: "nfu" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) in
    let: "$a1" := #false in
    (func_call #util.util #"CountBoolMap"%go) "$a0" "$a1") in
    do:  ("nfu" <-[#uint64T] "$r0");;;
    (if: (~ (let: "$a0" := (![#uint64T] "nfu") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"hcquorum" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    let: "$r0" := (map.make #uint64T #boolT) in
    do:  ((struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp")) <-[#(mapT uint64T boolT)] "$r0");;;
    let: "$r0" := GPP_UNPREPARING in
    do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0");;;
    return: (#true)).

(* go: gcoord.go:900:27 *)
Definition GroupPreparer__collectFastDecision : val :=
  rec: "GroupPreparer__collectFastDecision" "gpp" "rid" "b" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "b" := (mem.alloc "b") in
    let: "rid" := (mem.alloc "rid") in
    let: "$r0" := (![#boolT] "b") in
    do:  (map.insert (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"frespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid") "$r0")).

(* go: gcoord.go:904:27 *)
Definition GroupPreparer__collectValidation : val :=
  rec: "GroupPreparer__collectValidation" "gpp" "rid" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "rid" := (mem.alloc "rid") in
    let: "$r0" := #true in
    do:  (map.insert (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"vdm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid") "$r0")).

(* go: gcoord.go:908:27 *)
Definition GroupPreparer__in : val :=
  rec: "GroupPreparer__in" "gpp" "phase" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "phase" := (mem.alloc "phase") in
    return: ((![#uint64T] (struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp"))) = (![#uint64T] "phase"))).

(* go: gcoord.go:912:27 *)
Definition GroupPreparer__processFastPrepareResult : val :=
  rec: "GroupPreparer__processFastPrepareResult" "gpp" "rid" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    let: "rid" := (mem.alloc "rid") in
    (if: let: "$a0" := (![#uint64T] "res") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryResign" (![#ptrT] "gpp")) "$a0"
    then return: (#false)
    else do:  #());;;
    (if: (![#uint64T] "res") = tulip.REPLICA_FAILED_VALIDATION
    then
      do:  (let: "$a0" := (![#uint64T] "rid") in
      let: "$a1" := #false in
      (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"collectFastDecision" (![#ptrT] "gpp")) "$a0" "$a1");;;
      let: "aborted" := (mem.alloc (type.zero_val #boolT)) in
      let: "$r0" := ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryFastAbort" (![#ptrT] "gpp")) #()) in
      do:  ("aborted" <-[#boolT] "$r0");;;
      (if: ![#boolT] "aborted"
      then return: (#false)
      else do:  #());;;
      (if: (~ (let: "$a0" := GPP_VALIDATING in
      (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"))
      then return: (#false)
      else do:  #());;;
      return: ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryBecomeUnpreparing" (![#ptrT] "gpp")) #())
    else do:  #());;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    let: "$a1" := #true in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"collectFastDecision" (![#ptrT] "gpp")) "$a0" "$a1");;;
    (if: (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryFastPrepare" (![#ptrT] "gpp")) #()
    then return: (#false)
    else do:  #());;;
    (if: (~ (let: "$a0" := GPP_VALIDATING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"collectValidation" (![#ptrT] "gpp")) "$a0");;;
    return: ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryBecomePreparing" (![#ptrT] "gpp")) #())).

(* go: gcoord.go:951:27 *)
Definition GroupPreparer__processValidateResult : val :=
  rec: "GroupPreparer__processValidateResult" "gpp" "rid" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    let: "rid" := (mem.alloc "rid") in
    (if: let: "$a0" := (![#uint64T] "res") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryResign" (![#ptrT] "gpp")) "$a0"
    then return: (#false)
    else do:  #());;;
    (if: (![#uint64T] "res") = tulip.REPLICA_FAILED_VALIDATION
    then return: (#false)
    else do:  #());;;
    (if: (~ (let: "$a0" := GPP_VALIDATING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"))
    then return: (#false)
    else do:  #());;;
    do:  (let: "$a0" := (![#uint64T] "rid") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"collectValidation" (![#ptrT] "gpp")) "$a0");;;
    return: ((method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryBecomePreparing" (![#ptrT] "gpp")) #())).

(* go: gcoord.go:973:27 *)
Definition GroupPreparer__processPrepareResult : val :=
  rec: "GroupPreparer__processPrepareResult" "gpp" "rid" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    let: "rid" := (mem.alloc "rid") in
    (if: let: "$a0" := (![#uint64T] "res") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryResign" (![#ptrT] "gpp")) "$a0"
    then return: (#())
    else do:  #());;;
    (if: (~ (let: "$a0" := GPP_PREPARING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"))
    then return: (#())
    else do:  #());;;
    let: "$r0" := #true in
    do:  (map.insert (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid") "$r0");;;
    let: "n" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) in
    map.len "$a0")) in
    do:  ("n" <-[#uint64T] "$r0");;;
    (if: let: "$a0" := (![#uint64T] "n") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"cquorum" (![#ptrT] "gpp")) "$a0"
    then
      let: "$r0" := GPP_PREPARED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0")
    else do:  #())).

(* go: gcoord.go:996:27 *)
Definition GroupPreparer__processUnprepareResult : val :=
  rec: "GroupPreparer__processUnprepareResult" "gpp" "rid" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    let: "rid" := (mem.alloc "rid") in
    (if: let: "$a0" := (![#uint64T] "res") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryResign" (![#ptrT] "gpp")) "$a0"
    then return: (#())
    else do:  #());;;
    (if: (~ (let: "$a0" := GPP_UNPREPARING in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"in" (![#ptrT] "gpp")) "$a0"))
    then return: (#())
    else do:  #());;;
    let: "$r0" := #true in
    do:  (map.insert (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) (![#uint64T] "rid") "$r0");;;
    let: "n" := (mem.alloc (type.zero_val #uint64T)) in
    let: "$r0" := (s_to_w64 (let: "$a0" := (![#(mapT uint64T boolT)] (struct.field_ref #GroupPreparer #"srespm"%go (![#ptrT] "gpp"))) in
    map.len "$a0")) in
    do:  ("n" <-[#uint64T] "$r0");;;
    (if: let: "$a0" := (![#uint64T] "n") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"cquorum" (![#ptrT] "gpp")) "$a0"
    then
      let: "$r0" := GPP_ABORTED in
      do:  ((struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")) <-[#uint64T] "$r0")
    else do:  #())).

(* go: gcoord.go:1019:27 *)
Definition GroupPreparer__processQueryResult : val :=
  rec: "GroupPreparer__processQueryResult" "gpp" "res" :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    let: "res" := (mem.alloc "res") in
    do:  (let: "$a0" := (![#uint64T] "res") in
    (method_call #gcoord.gcoord #"GroupPreparer'ptr" #"tryResign" (![#ptrT] "gpp")) "$a0")).

(* go: gcoord.go:1024:27 *)
Definition GroupPreparer__getPhase : val :=
  rec: "GroupPreparer__getPhase" "gpp" <> :=
    exception_do (let: "gpp" := (mem.alloc "gpp") in
    return: (![#uint64T] (struct.field_ref #GroupPreparer #"phase"%go (![#ptrT] "gpp")))).

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [("Start"%go, Start); ("mkGroupCoordinator"%go, mkGroupCoordinator); ("mkGroupReader"%go, mkGroupReader); ("mkGroupPreparer"%go, mkGroupPreparer)].

Definition msets' : list (go_string * (list (go_string * val))) := [("GroupCoordinator"%go, []); ("GroupCoordinator'ptr"%go, [("Abort"%go, GroupCoordinator__Abort); ("Attach"%go, GroupCoordinator__Attach); ("AttachedWith"%go, GroupCoordinator__AttachedWith); ("ChangeLeader"%go, GroupCoordinator__ChangeLeader); ("Commit"%go, GroupCoordinator__Commit); ("Connect"%go, GroupCoordinator__Connect); ("Finalized"%go, GroupCoordinator__Finalized); ("GetConnection"%go, GroupCoordinator__GetConnection); ("GetLeader"%go, GroupCoordinator__GetLeader); ("NextPrepareAction"%go, GroupCoordinator__NextPrepareAction); ("Prepare"%go, GroupCoordinator__Prepare); ("PrepareSession"%go, GroupCoordinator__PrepareSession); ("Read"%go, GroupCoordinator__Read); ("ReadSession"%go, GroupCoordinator__ReadSession); ("Receive"%go, GroupCoordinator__Receive); ("RegisterFinalization"%go, GroupCoordinator__RegisterFinalization); ("ResendSession"%go, GroupCoordinator__ResendSession); ("ResponseSession"%go, GroupCoordinator__ResponseSession); ("Send"%go, GroupCoordinator__Send); ("SendAbort"%go, GroupCoordinator__SendAbort); ("SendCommit"%go, GroupCoordinator__SendCommit); ("SendFastPrepare"%go, GroupCoordinator__SendFastPrepare); ("SendPrepare"%go, GroupCoordinator__SendPrepare); ("SendQuery"%go, GroupCoordinator__SendQuery); ("SendRead"%go, GroupCoordinator__SendRead); ("SendRefresh"%go, GroupCoordinator__SendRefresh); ("SendUnprepare"%go, GroupCoordinator__SendUnprepare); ("SendValidate"%go, GroupCoordinator__SendValidate); ("ValueResponded"%go, GroupCoordinator__ValueResponded); ("WaitUntilPrepareDone"%go, GroupCoordinator__WaitUntilPrepareDone); ("WaitUntilValueReady"%go, GroupCoordinator__WaitUntilValueReady); ("attachedWith"%go, GroupCoordinator__attachedWith); ("processFinalizationResult"%go, GroupCoordinator__processFinalizationResult); ("waitOnResendSignal"%go, GroupCoordinator__waitOnResendSignal)]); ("GroupReader"%go, []); ("GroupReader'ptr"%go, [("clearVersions"%go, GroupReader__clearVersions); ("cquorum"%go, GroupReader__cquorum); ("pickLatestValue"%go, GroupReader__pickLatestValue); ("processReadResult"%go, GroupReader__processReadResult); ("read"%go, GroupReader__read); ("reset"%go, GroupReader__reset); ("responded"%go, GroupReader__responded)]); ("GroupPreparer"%go, []); ("GroupPreparer'ptr"%go, [("action"%go, GroupPreparer__action); ("attach"%go, GroupPreparer__attach); ("collectFastDecision"%go, GroupPreparer__collectFastDecision); ("collectValidation"%go, GroupPreparer__collectValidation); ("cquorum"%go, GroupPreparer__cquorum); ("fquorum"%go, GroupPreparer__fquorum); ("getPhase"%go, GroupPreparer__getPhase); ("hcquorum"%go, GroupPreparer__hcquorum); ("in"%go, GroupPreparer__in); ("processFastPrepareResult"%go, GroupPreparer__processFastPrepareResult); ("processPrepareResult"%go, GroupPreparer__processPrepareResult); ("processQueryResult"%go, GroupPreparer__processQueryResult); ("processUnprepareResult"%go, GroupPreparer__processUnprepareResult); ("processValidateResult"%go, GroupPreparer__processValidateResult); ("ready"%go, GroupPreparer__ready); ("tryBecomePreparing"%go, GroupPreparer__tryBecomePreparing); ("tryBecomeUnpreparing"%go, GroupPreparer__tryBecomeUnpreparing); ("tryFastAbort"%go, GroupPreparer__tryFastAbort); ("tryFastPrepare"%go, GroupPreparer__tryFastPrepare); ("tryResign"%go, GroupPreparer__tryResign)])].

#[global] Instance info' : PkgInfo gcoord.gcoord :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [sync.sync; primitive.primitive; grove_ffi.grove_ffi; message.message; params.params; quorum.quorum; tulip.tulip; util.util];
  |}.

Definition initialize' : val :=
  rec: "initialize'" <> :=
    globals.package_init gcoord.gcoord (λ: <>,
      exception_do (do:  util.initialize';;;
      do:  tulip.initialize';;;
      do:  quorum.initialize';;;
      do:  params.initialize';;;
      do:  message.initialize';;;
      do:  grove_ffi.initialize';;;
      do:  primitive.initialize';;;
      do:  sync.initialize')
      ).

End code.
End gcoord.
